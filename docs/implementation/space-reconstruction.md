# 실제 도면 공간 복원 — 실행 계획 및 1차 구현

## 목표

항목별 사용자 확인이나 외부 인식 API 없이 도면 구조를 복원한다. 문·창문의 빈 구간을 가짜 벽으로 닫지 않는다. 실제 도면 완성률로 평가하며 배포는 별도 시점에 진행한다.

## 구현 완료: 연결 구조와 짧은 벽

- 실제 교차점에서 선분을 분리해 T자·십자 접합을 같은 좌표로 연결한다.
- 평면 그래프의 방향별 면 순회로 공유 벽이 있는 여러 실의 외곽과 내부 벽을 분리한다.
- 반대 방향으로 중복된 선은 제거한다. 부분적으로 겹치는 선과 순환 영역 사이의 불명확한 연결은 보류한다.
- 오목한 외곽과 중정을 가로지르는 외부 선 차단을 유지한다.
- 짧은 선을 일괄 제거하지 않고, 양끝이 연결되고 긴 구조선 네트워크에 속한 연결부는 유지한다. 인식된 문자 상자 내부의 선과 독립 글자 고리는 제외한다.
- 교차점에서 벽이 분할되더라도 원래 전체 길이 치수로 축척을 검증한다.

검증: 160개 테스트 통과, 빌드 성공. 공유 방·교차 접합·사선 분할·순서/방향 불변성·짧은 꺾임·축척 연결을 검사했다. 입력 후보의 사선 연결 처리는 지원하지만 래스터에서 사선을 검출하는 기능은 아직 없다. 200개 벽 상한을 넘는 분할은 보류한다.

## 실제 도면 재검증

동일한 ANCA 1페이지와 Espacio 2페이지, 각 3개 밝기 조건으로 다시 실행했다. 전체 생성은 여전히 0/3이다. Espacio 2의 구조 후보가 조건별 24/30/31개에서 25/31/34개로 바뀌었다. 후보 증가를 정확도 향상이나 성공으로 집계하지 않는다. 세부 결과는 `../validation/2026-09-11-topology-retest.json`에 있다.

## 다음 구현 순서

1. **개구부를 가진 경계 모델**: 실제 벽과 바닥 경계 연결부를 별도 타입으로 정의한다. 문·창문의 양끝, 유형, 도면 근거를 보존한다. 저장/불러오기·2D/3D·수정·Undo까지 연결한 뒤 인식 결과를 적용한다. 문을 통과할 수 있는 경계가 벽으로 렌더되거나 작품 설치면이 되면 실패다.
2. **이중선 및 선 후보 유실 처리**: 평행한 두 선을 거리만으로 합치지 않고 공통 구간·끝단·연결 관계를 검사한다. 가까운 복도 벽 두 개가 합쳐지면 실패다. 100개 후보 상한에서 짧은 구조 근거를 잃지 않도록 구조용/치수용 후보를 구분한다.
3. **문·창문 구조 근거 결합**: 개구부 양끝과 문짝/회전호/창선 등 기하 근거를 검사한다. 문자 위치만으로 물리적 폭을 만들지 않는다. 닫히지 않은 경계는 임의 보간하지 않는다.
4. **사선과 실제 정답 검증**: 사선 검출을 추가하고 좌표를 주석한 도면으로 벽·개구부 누락/오검출을 비교한다. 같은 3페이지 회귀 검사와 별도 도면 검증을 병행한다.

전체 단위 상속 및 회전 숫자는 치수 인식 트랙과 연동해야 하며, 공간 복원만으로 해결된 것으로 간주하지 않는다. 웹 배포는 이번 작업에서 하지 않는다.


## 개구부 경계 모델 및 자동 연결

문/창문을 실제 벽과 분리된 개구부로 저장하고 벽 끝점에 연결했다. 바닥 경계에만 가상 연결을 사용하며, 작품 설치면이나 3D 벽 메시로 생성하지 않는다. 2D/3D는 개구부를 점선으로 표시한다. 벽 수정 시 연결점을 따라가고, 삭제 시 해당 개구부를 제거하며 Undo/Redo로 복원한다. JSON 검증은 잘못된 참조/중복 ID/0 길이를 거부한다.

문·창문 표기와 같은 선상에서 서로 마주 보는 벽 끝점의 틈이 유일하게 연결될 때 자동 후보를 생성한다. OCR 90 미만, 여러 후보 충돌, 다른 선과 교차, 검증 범위 밖 폭은 보류한다. 폭은 문자 상자가 아니라 실제 벽 끝점으로 계산한다. 문짝 회전호·창틀·상인방/창턱 높이는 아직 추론하지 않는다. 문·창문 후보 유형과 위치도 3회 자체 검증 결과 비교에 포함했다.

169개 자동 테스트와 빌드 통과. 별도 로컬 포트의 브라우저에서 개구부 프로젝트 불러오기, 3D의 열린 출입구와 이어진 바닥, 평면 전환 및 오류 없음 확인. 공개 도면 3페이지 재검증은 여전히 완전 생성 0/3이다. `docs/validation/2026-09-11-openings-retest.json` 참조. 웹 배포하지 않음. 전체 다섯 목표는 계속 미완료 상태이며 다음 구현은 이중선·선 후보 유실 처리다.


## 후보 보존·이중선·전체 단위 선언

자동 분석은 최대 500개 선 후보를 보존하고 수동 도구는 기존 100개 기본값을 유지한다. 저장 검증도 500개까지 허용한다. 양끝을 잇는 선이 유일하게 확인되는 평행 이중선만 중심선 후보로 변환한다. 끝이 열린 평행선, 복도 폭의 간격, 경쟁하는 끝단 근거는 합치지 않는다. 이 판정은 구조 후보 판정이며 길쭉한 가구/창틀의 의미 분류까지 해결한 것은 아니다.

페이지 전체의 명시적 단위 선언(예: All measurements in cm, 단위: mm)을 표기로 보존하고 단위 없는 숫자의 자동 축척 계산에 적용한다. 원래 숫자와 개별 명시 단위를 유지하며 서로 충돌하는 전체 단위, 낮은 OCR 점수, 특정 객체 치수 문구는 기본 단위로 사용하지 않는다. Espacio 실제 두 페이지에서 cm 선언 보존을 확인했다.

179개 테스트와 빌드 통과. 동일 공개 도면 재검증은 전체 생성 0/3으로 유지되며, 일부 입력은 500개 상한에도 도달했다. 후보 수 증가는 정확도 개선으로 집계하지 않는다. `docs/validation/2026-09-11-paired-units-retest.json`에 결과를 보존했다. 외부 API와 웹 배포는 사용하지 않았다. 다음은 세로/회전 숫자와 구조선 근거 보완이다.


## 전체/부분 치수 연결 및 두꺼운 선 교차점 보정

연속된 여러 벽의 전체 치수, 하나의 벽 중간에 놓인 부분 치수를 치수선 및 양끝 보조선으로 연결한다. 실제 벽이 없는 구간은 연결하지 않는다. 부분 치수가 전체 구간을 빈틈없이 구성할 때 합계를 대조하고 2%를 넘는 불일치를 보류한다. 겹치거나 경쟁하는 부분 치수는 합산하지 않는다. 자동 공간 생성과 축척 검증에 통합했다.

두꺼운 래스터 선 끝의 외곽 픽셀과 수직 벽 중심선 사이 오프셋을 선 두께와 상한 12px로 보정한다. 수평·수직 방향을 유지하고, 경쟁하는 교차점/문 크기의 틈/선분 붕괴는 보정하지 않는다. 고정 4px 근접 연결 전에 처리한다.

193개 테스트와 빌드 통과. 동일한 공개 도면 재검증은 전체 생성 0/3이다. Espacio에서 구조 후보가 늘었지만 정답 구조 재현율이나 성공률 향상으로 집계하지 않는다. 결과는 `docs/validation/2026-09-11-dimension-spans-retest.json`에 보존했다. 다음은 외곽 후보와 가구·범례 선 분리 및 사선 검출이다. 전체 다섯 목표는 미완료이며 외부 API/웹 배포는 사용하지 않는다.


## 래스터 벽 띠 병합·회전 시설 표기·계단 검출 범위

두꺼운 벽이 여러 평행 띠로 분리되었을 때 원본 이진 픽셀의 97% 이상이 채워진 구간만 병합한다. 16px 두께 상한, 길이 90% 이상 겹침과 끝점 차이 제한을 적용하며 빈 간격이 있는 평행선은 유지한다. 회전 OCR에서 얻은 90점 이상 시설 표기도 보존한다.

계단 표기 주변의 규칙적인 반복 단 선 3개 이상으로 검출 범위를 만든다. 문자 상자를 면적으로 사용하지 않는다. 최소 두 분석 조건이 일치하고 나머지 검출 결과에 모순이 없어야 분석 화면과 평면도에 표시한다. JSON 저장/불러오기에서 선·표기 참조와 범위를 검증한다. 현재는 검출한 단 선의 범위 표시이며 계단 전체 외곽 복원, 3D 계단 모델, 작품 배치 차단은 아직 구현하지 않았다.

202개 자동 테스트 및 빌드 통과. 실제 Espacio 1페이지에서 출입문·창문·계단 표기를 얻었고 계단 영역 1개가 세 조건에서 일치했다. 공개 도면 3페이지의 전체 공간 자동 생성은 여전히 0/3이다. 결과는 `docs/validation/2026-09-11-stair-regions-retest.json`에 저장했다. 로컬 Node Canvas/OCR 검증이며 이번 표시 변경의 브라우저 검증은 별도다. 외부 인식 API 및 웹 배포 없이 구현했다.

다음 우선순위는 실제 도면 외곽의 누락 선 복원과 가구·계단 단 선의 벽 오분류 제거다. 이후 사선, 완전한 설치 제외 영역 및 자동 페이지 선택을 검증한다. M0와 다섯 세부 목표는 계속 미완료다.


## 계단 단 선의 벽 오분류 제거

계단 표기와 규칙적 단 선의 결합 근거가 있을 때, 가운데 단 선을 이중벽 병합·구조 그래프 구성 전에 벽 후보에서 제외한다. 양쪽 끝 선은 계단실 벽일 수 있어 유지하며 다른 선으로 같은 방향으로 이어지는 단 선도 제거하지 않는다. 원본 분석 선은 보존한다. 자동 공간 채택에는 기존의 여러 검출 조건 간 일치 검증이 계속 적용된다.

204개 테스트와 빌드를 통과했다. 실제 Espacio 1페이지에서 세 조건 모두 가운데 계단 선 1개가 벽 후보에서 빠져 후보 수가 32/34/39 → 31/33/38로 변했다. 다른 두 페이지는 변하지 않았다. 전체 공간 생성은 여전히 0/3이며 후보 수 감소를 전체 구조 정확도로 집계하지 않는다. 결과는 `docs/validation/2026-09-11-stair-tread-retest.json`에 보존했다. 외부 API·웹 배포 없이 진행했으며 다음 핵심 과제는 외곽 누락 선과 사선 검출이다.


## 사선 래스터 검출과 모서리 연결

수평·수직 띠를 제거한 잉크의 연결 성분에 주축 직선을 맞춰 사선 후보를 검출한다. 길이·폭·횡방향 편차·연속 구간 조건을 함께 확인하며 서로 떨어진 성분을 합치지 않는다. 꺾인 선 묶음은 직선으로 바꾸지 않는다. 이것은 사선 후보 검출이며 가구와 벽의 의미 구분이나 서로 연결된 복잡한 사선망 전체의 분해까지 해결한 것은 아니다.

두꺼운 구조 후보의 사선 교차점도 기존 12px 이내 두께 기반 오차 범위에서 연결한다. 얕은 교차각, 경쟁하는 교차점, 큰 틈은 보정하지 않는다. 얇은 치수 보조선은 구조 모서리 보정에 참여시키지 않는다. 사선 후보가 수직 띠 병합에 들어가는 경우도 차단했다.

209개 테스트와 빌드 통과. 양·음 방향과 수평/수직에 가까운 각도, 실제 틈 보존, 꺾인 성분 거부를 검증했다. 사선 외벽을 포함한 합성 래스터 방에서 벽 검출→모서리 연결→독립 치수 근거 2개→축척 있는 닫힌 공간 생성까지 통과했다. 합성 사례는 실제 도면 완료율에 포함하지 않는다.

공개 도면 재검증 결과는 `docs/validation/2026-09-11-diagonal-retest.json`이며 전체 자동 생성은 여전히 0/3이다. 새로 검출된 사선 후보의 실도면 정답 여부는 별도 주석 검증이 필요하다. 사용자가 체크하는 단계는 추가하지 않았다. 외부 API/웹 배포 없이 진행했다. 다음은 외곽 누락 구간의 원본 픽셀 근거와 복잡한 선망 분해다. 전체 목표는 미완료다.


## 연결 근거가 있는 얇은 구조선 보존

1~3px의 긴 선도 양끝이 서로 다른 두꺼운 구조 후보에 직접 연결될 때 유지한다. 얇은 선끼리의 연결은 채택 근거로 쓰지 않으며, 독립 구조 시작점도 두꺼운 선으로 제한한다. 문자 상자 내부 선 제거와 기존 연결 그래프 검증을 유지한다. 짧은 사선 래스터 조각이 재유입되지 않도록 긴 선에만 적용한다.

212개 테스트와 빌드 통과. 얇은 외곽선이 포함된 합성 공간의 전체 생성과 치수 적용을 검증했다. 실제 Espacio 1페이지 후보는 32/33/38→33/36/39, 2페이지는 37/37/45→40/39/52로 변했다. 전체 생성은 여전히 0/3이며 추가 후보의 정답 여부를 전체 정확도 개선으로 주장하지 않는다. 결과는 `docs/validation/2026-09-11-thin-boundary-retest.json`에 기록했다.

추가 원인 확인: Espacio 1페이지 아래쪽에는 2400px 좌표 기준 (123.43,1492.79)~(1263.43,1492.79)의 연속 얇은 선이 원본 검출 결과에 존재한다. 왼쪽 벽 중심 x≈132보다 바깥으로 연장되어 현재 4px 직접 연결 조건을 통과하지 못한다. 선 누락이 아니라 끝점 돌출 및 일부 구간의 두꺼운 평행 띠 중복 문제다. 다음에는 실제 교차점 기반 끝점 보정과 부분적으로 겹치는 띠의 픽셀 근거를 처리해야 한다. 외부 API 및 웹 배포 없이 진행했으며 전체 목표는 미완료다.


## 얇은 경계 끝점 돌출 보정

얇고 긴 선의 끝 12px 이내에 두꺼운 선과 실제 교차점이 유일하게 있을 때 돌출 부분만 줄인다. 선을 연장하지 않으며, 상대 선의 범위 밖 교차점, 여러 교차점, 얕은 교차각, 얇은 보조선은 근거로 사용하지 않는다. 원본 검출 선은 보존한다.

216개 테스트 및 빌드 통과. 돌출된 얇은 외곽이 포함된 합성 공간의 전체 생성과 축척 적용을 확인했다. 실제 Espacio 1페이지의 기준 조건(155)에서 아래쪽 긴 경계가 (132,1492.79)~(1261.71,1492.79)로 구조 후보에 복원됐다. 동일 구간의 두꺼운 평행 띠는 여전히 남아 있어 중복 해소가 필요하다. 전체 자동 생성은 0/3이며, 특정 경계 복원과 전체 정확도를 구분한다. 결과: `docs/validation/2026-09-11-thin-overrun-retest.json`.

다음은 원본 픽셀에 근거한 부분 평행 띠 병합이다. 외부 API 및 웹 배포 없이 진행했고 전체 목표는 미완료다.


## 부분 평행 띠의 원본 픽셀 기반 중복 제거

긴 선에 완전히 포함된 평행 조각은 두 띠 사이의 전체 해당 픽셀 구간이 97% 이상 채워지고 합친 폭이 16px 이내일 때만 제거한다. 긴 선의 실제 좌표와 두께는 유지한다. 같은 선상에서 치수 보조선이 벽보다 길어지는 경우는 제외하며, 빈 틈이 있거나 일부만 겹친 선도 유지한다.

두꺼운 조각에서 얻은 구조 지지는 `solidSupportThicknessPx`로 별도 보존하고 원본 좌표 변환 시 함께 환산한다. 구조 후보 구성에서만 이 근거를 사용하며 실제 벽 두께로 단정하지 않는다. JSON 검증에도 유한값·범위를 추가했다. 기존 래스터 공간 생성 회귀 테스트가 밝힌 보조선 오채택을 수정했다.

219개 테스트와 빌드 통과. 실제 Espacio 1페이지 기준 조건(155)에서 아래쪽 경계는 (132,1492.79)~(1261.71,1492.79) 한 선으로 유지되고 이전 두꺼운 평행 중복은 제거됐다. 전체 자동 생성은 여전히 0/3이다. 결과는 `docs/validation/2026-09-11-contained-bands-retest.json`이며 전체 목표는 미완료다. 다음은 남은 출입구 주변 경계와 외부 비구조 선의 분리다. 외부 API와 웹 배포 없이 진행했다.


## 창틀이 연결된 창문 개구부

창문 표기와 일치하는 벽 끝점 사이에 평행 창틀 한 선·양끝 짧은 연결선 두 개가 있을 때, 이 세 선을 설치 가능한 벽에서 제외하고 창문 개구부로 변환한다. 양쪽 실제 벽 끝점으로 폭을 정하며 문자 상자를 크기로 사용하지 않는다. 연결 깊이 30px 이내, 평행·폭 일치, 유일한 해석을 요구하고 다른 벽의 분기·교차, 불완전한 창틀, 낮은 OCR 점수, 중복 표기는 보류한다. 기존 개구부 저장·3D/평면 표시·다중 조건 검증을 사용한다.

222개 테스트와 빌드 통과. 창틀 포함 합성 공간에서 창문 개구부 및 전체 공간 생성, JSON 검증을 통과했다. 실제 Espacio 1페이지는 기준 조건155에서 창문 1개를 검출했지만 125/190에서는 미확보했다. 따라서 실도면에서 안정적인 자동 채택을 달성한 것으로 간주하지 않는다. 전체 생성은 여전히 0/3이다. 결과는 `docs/validation/2026-09-11-framed-windows-retest.json`에 보존했다.

다음은 조건별 창틀 선 누락과 정문 주변의 남은 경계 복원이다. 다섯 목표는 계속 미완료이며 외부 API와 웹 배포 없이 진행했다.


## 창틀/빈 틈 표현 간 개구부 검출 안정화

창틀 기반 변환과 빈 틈 기반 변환을 공통 개구부 해석 함수로 묶었다. 원본 선 조건에 따라 창틀이 사라져도 같은 실제 벽 끝점의 창문으로 해석되는지 검증한다. 창틀 판정의 평행/정렬 오차는 2px까지 허용하며 더 크게 어긋난 경계는 보류한다. 실제 Espacio 1페이지의 190 조건은 약 1.7px 차이로 기존 1px 조건에 막혀 있었다.

224개 테스트와 빌드 통과. 실제 Espacio 1페이지의 동일한 뒤쪽 창문이 125 조건에서는 빈 틈, 155/190에서는 창틀로 인식되어 세 조건 모두 같은 개구부 후보를 얻었다. 시작점 차이는 최대 약 1.7px, 끝점 차이는 약 0.86px이다. 이는 해당 개구부의 조건 간 일치이며 전시장 전체 완료를 뜻하지 않는다. 전체 공간 생성은 여전히 0/3이다. 결과는 `docs/validation/2026-09-11-window-stability-retest.json`에 보존했다.

다음은 정문 주변의 누락된 구조선과 다른 개구부의 해석이다. 외부 API 및 웹 배포는 사용하지 않았으며 전체 목표는 미완료다.


## 실도면 구조 구간 정답 및 단계별 누락 측정

Espacio 1페이지 원본을 직접 확인해 선택한 벽 구간 6개를 `docs/validation/espacio-selected-structure.json`에 기록했다. 원본 SHA-256·페이지·크기 일치 시에만 점수를 계산한다. 4px 위치 오차 내 평행 후보의 실제 겹친 구간 합집합으로 길이 커버리지를 계산하며 중복 선을 중복 집계하지 않는다. raw(선 검출)와 selected(구조 선택)를 분리한다. 95% 이상을 구간 검출로 판정한다. 전체 도면 정답이나 실제 mm 치수 정확도가 아닌 선택 구간의 픽셀 기준 측정이다.

226개 테스트 및 빌드 통과. 세 조건 모두 정문 위 수직/수평 연결 2개는 raw 100%, selected 0%였다. 나머지 선택 구간 4개는 모두 95% 이상 유지됐다. 즉 이 구간 실패는 원본 선 누락이 아니라 얇은 연결선 묶음의 구조 선택 문제다. 전체 공간 생성은 계속 0/3이다. 결과는 `docs/validation/2026-09-11-structure-coverage-retest.json`에 보존했다.

다음 구현은 두꺼운 외벽부터 문설주까지 이어지는 얇은 연결선 묶음을 처리하는 것이다. 문설주/개구부의 의미 근거를 함께 사용해야 하며 일반 치수 보조선 묶음을 벽으로 승격하지 않아야 한다. 외부 API와 웹 배포 없이 진행했으며 다섯 목표는 미완료다.


## 모서리 출입구 및 연결선 묶음 보존

한쪽 벽은 문 방향과 직각이고 다른 벽은 문 방향과 평행한 모서리 출입구를, 확실한 문 표기와 실제 벽 끝점으로 검출한다. 문 구간을 실제 선이 막거나 해석이 경쟁하면 보류한다. 문 끝점에 연결된 짧은 선 묶음은 두꺼운 구조 시작점까지 이어지는 유일한 최대 4개 선 경로가 있을 때 유지한다. 근거 없는 얇은 선끼리의 연결은 여전히 채택하지 않는다.

진단 수정: 정문 연결선은 조건155에서 두께 자체도 3px 이상이었다. 직접 원인은 짧은 종단 선을 제거하는 규칙과 모서리 문 형태를 지원하지 않는 규칙의 결합이었다. 조건에 따라 더 얇은 선도 있으므로 출입구 의미와 구조 연결을 함께 사용한다.

230개 테스트 및 빌드 통과. 모서리 문이 포함된 합성 공간의 개구부 생성·전체 구조 생성·JSON 검증을 통과했다. 실제 Espacio 1페이지 정문 위 두 구간은 조건125/155에서 기존 selected 0%→약98.5~100%로 복원됐고 정문 개구부도 검출됐다. 이 두 조건에서 선택한 구조 구간 6개 모두 95% 이상이다. 조건190의 정문 연결선은 아직 누락된다. 전체 생성은 계속 0/3이며 전체 정답 복원으로 주장하지 않는다. 결과: `docs/validation/2026-09-11-corner-doors-retest.json`.

다음은 나머지 조건의 정문 형상 차이와 계단 쪽 열린 경계·내부 개구부 처리다. 외부 API·웹 배포 없이 진행했으며 전체 목표는 미완료다.


## 계단 검출 범위의 3D 표시와 작품 겹침 경고

축척이 확정된 도면의 계단 검출 범위를 실제 좌표로 변환해 3D 바닥에 표시한다. 작품의 회전·폭·깊이·프레임과 벽 앞뒤 설치면을 반영한 평면 점유 다각형으로 겹침을 검사한다. 겹치면 3D 작품 위와 속성창에 자동 경고하며 이동 후 다시 계산한다. 벽 치수 변경 미리보기의 경고에도 포함한다. 축척 미확정 또는 제외된 계단 표기는 실제 좌표 제한으로 사용하지 않는다.

이는 검출된 범위의 표시와 배치 경고다. 저장/배치를 강제 차단하지 않으며 계단 전체 형상·높이·법정 이격거리를 추정하지 않는다. 에어컨·소화전 등 다른 설비의 실제 영역 자동 인식은 여전히 미완료다.

233개 테스트 및 빌드 통과. 로컬 브라우저에서 검증 프로젝트 불러오기, 3D 계단 영역 및 작품 겹침 경고 표시, 작품 위치 1400→2800mm 이동 후 3D/속성창 경고 해제, 브라우저 오류 없음을 확인했다. 단위 테스트는 회전 다각형 판정, 벽 뒷면, 이동, 미확정 축척과 제외 표기를 검증했다. 이번 변경은 실도면 인식기를 바꾸지 않았으며 마지막 실제 전체 생성 결과는 0/3이다. 외부 API와 웹 배포는 사용하지 않았다. 전체 목표는 계속 미완료다.


## 여러 페이지 PDF 자동 분석·선택

다중 페이지 PDF의 기본값을 첫 페이지 수동 선택에서 자동 선택으로 바꿨다. 페이지를 순서대로 렌더·로컬 분석하며 자체 공간 검증을 통과한 첫 페이지에서 멈춘다. 실패한 페이지는 건너뛰고 계속한다. 검증된 공간이 없으면 구조 후보와 숫자 근거 점수가 가장 높은 페이지를 미확정 후보로 표시한다. 동점은 앞 페이지를 유지한다. 이 점수는 평면도 여부의 검증된 분류 정확도가 아니다. 처리한 페이지 수·선택 페이지·실패 건수를 안내한다.

전체 페이지를 동시에 보관하지 않고 가장 좋은 결과만 유지해 메모리를 제한한다. 취소/닫기 및 오래된 요청 결과의 반영을 차단한다. 모든 인식이 실패해도 읽을 수 있었던 원본을 보존하며 즉시 같은 실패 분석을 무한 반복하지 않는다. 단일 페이지 흐름과 수동 페이지 변경은 유지한다.

237개 테스트 및 빌드 통과. 실제 Espacio 두 페이지 PDF를 브라우저에서 업로드한 뒤 선택 조작 없이 2페이지까지 분석하고 2페이지를 미확정 후보로 표시하는 것을 확인했다. 공간 자체 검증 실패 상태를 유지했고 브라우저 오류가 없었다. 단위 테스트는 페이지 실패 이후 계속 처리, 첫 검증 성공 시 중단, 후보/동점 처리, 전체 실패와 원본 보존, 분석 중 취소를 검증한다.

이 변경은 다중 페이지 선택 자동화의 첫 단계다. 여러 층 병합, 자동 영역 분리와 페이지 방향 정규화, 최적 페이지 분류 정확도는 아직 미완료다. 마지막 실도면 전체 공간 생성은 0/3이며 다섯 목표는 계속 진행 중이다. 외부 API와 웹 배포 없이 진행했다.


## 계단 통로의 바닥 경계 처리

계단 반복 단 선의 검출 범위, 양쪽 연결선, 서로 마주 보는 실제 벽 끝점이 일치할 때 계단 통로를 검출한다. 계단 쪽 선들은 원본 분석에 보존하면서 전시장 바닥 경계를 만드는 벽에서는 제외한다. 계단의 폭을 문자 상자로 만들지 않고 실제 벽 끝점으로 통로 폭을 정한다. 범위 불일치·연결선 누락·경쟁하는 계단 해석은 보류한다.

개구부 모델에 stair-access를 추가해 바닥 경계만 이어주고 물리적인 벽이나 작품 설치면을 만들지 않는다. JSON 검증과 2D/3D 명칭에도 반영했다. 통로 바깥 계단 전체 형상이나 층간 연결을 자동 모델링한 것은 아니다.

240개 테스트와 빌드 통과. 합성 도면에서는 계단 통로 포함 전체 공간 생성·축척 적용·JSON 검증을 통과했다. 실제 Espacio 1페이지는 세 조건 모두 계단 통로 후보를 얻었다. 정문과 뒤쪽 창문 후보도 함께 처리하지만 전체 공간 생성은 여전히 0/3이다. 결과: `docs/validation/2026-09-11-stair-access-retest.json`. 다음은 남은 내부 선 접합과 개구부, 치수 근거 검증이다. 외부 API 및 웹 배포 없이 진행했으며 다섯 목표는 미완료다.


## T자 접합의 래스터 오차 보정

벽 끝점이 다른 두꺼운 선의 내부 교차점에서 조금 어긋난 경우 양쪽 방향을 유지한 채 유일한 교차점으로 보정한다. 12px 이내 두께 기반 상한, 충분한 교차각, 상대 선 내부 조건을 적용한다. 경쟁 교차점·큰 틈·상대 선 밖 연장·얇은 문자 선은 보정하지 않는다. 이후 기존 평면 그래프 단계가 T자 접점을 분할한다.

244개 테스트 및 빌드 통과. 오차가 있는 내부 가벽의 전체 공간 생성과 치수 보존을 검증했다. 실제 Espacio 1페이지에서 조건125/155가 닫힌 외곽·내부 벽 그래프 검증을 통과했다. 자동 생성의 실패 원인은 이 두 조건에서 구조 불확정→독립 치수/축척 근거 부족으로 바뀌었다. 조건190과 다른 두 페이지는 여전히 구조 단계에서 실패한다.

검증 러너에 topologyResolved를 추가해 닫힌 구조와 치수 포함 최종 생성을 분리한다. 구조 검증 통과는 실제 정답 형상을 완전히 복원했다는 의미가 아니다. 전체 자동 생성은 계속 0/3이다. 결과: `docs/validation/2026-09-11-raster-junction-retest.json`. 다음은 치수선 없이 벽 옆에 기입된 숫자의 연결 근거와 도면 축척 일관성 검증이다. 외부 API 및 웹 배포 없이 진행했으며 다섯 목표는 미완료다.


## 치수선 없는 벽 옆 숫자와 비례 일관성

독립 치수선 근거가 부족할 때 숫자 방향·벽 중심 구간·거리로 유일하게 연결되는 인접 표기를 검사한다. 세 개 이상의 서로 다른 벽과 수평/수직 두 방향의 근거, 단위, OCR 신뢰도, 충돌 부재, 2% 이내 비율 일치를 모두 만족해야 하나의 축척으로 적용한다. 높이/두께 문구·중복/경쟁 벽·불확실한 값은 사용하지 않는다.

248개 테스트 및 빌드 통과. 치수선 없는 합성 비례 도면에서 인접 표기 세 개를 통한 공간 생성·JSON 검증을 통과했고, 불일치 표기에서는 자동 적용을 보류한다. 실제 Espacio 1페이지 기준 조건155에서 14개 인접 표기 후보를 연결했다. 예를 들어 4280mm/600px≈7.133mm/px, 1900mm/336.86px≈5.640mm/px이며 세로 후보들도 서로 다르다. 따라서 현재 연결 근거로는 단일 축척을 적용할 수 없다. 인접 표기 연결은 추론이므로 이를 모든 도면이 비례 없이 그려졌다는 확정 증거로 일반화하지 않는다.

실제 1페이지는 두 조건에서 구조 검증을 통과하지만 치수 비율 충돌로 전체 생성을 보류한다. 전체 성공은 계속 0/3이다. 결과: `docs/validation/2026-09-11-wall-annotations-retest.json`. 다음 주요 구현은 개별 치수 제약에 따른 좌표 재구성과 미기입 구간의 일관성 검증이다. 이미지의 단일 mmPerPixel만 바꾸는 방식으로는 해결되지 않는다. 외부 API와 웹 배포 없이 진행했으며 전체 목표는 미완료다.


## 개별 치수 제약의 상대 좌표 계산

`dimensionConstraints.ts`에 벽별 길이로 x/z 상대 좌표를 계산하는 그래프 검증을 추가했다. 같은 축 좌표는 소수점 여섯 자리로 정규화하며, 떨어진 좌표를 임의로 합치지 않는다. 연결된 치수마다 길이를 고정하고 전체/부분 치수의 순환 모순(0.001mm 초과), 좌표 순서 역전, 부적합한 입력을 검출한다. 연결되지 않은 성분은 각각 별도 원점을 가지며 미결정 상대 위치 수를 반환한다. 픽셀 비율로 누락 치수를 보간하지 않는다.

253개 테스트 및 빌드 통과. 실제 3페이지를 3가지 조건으로 재실행했다. Espacio 1페이지 조건155는 수평 15개·수직 18개의 연결 성분으로 남아 전체 좌표가 미결정이다. 단일 축척 불일치가 개별 치수 자체의 모순을 의미하지는 않으며, 이 페이지에서는 현재 연결된 제약 안의 순환 모순은 없었다. Espacio 2페이지 조건155/190에서는 연결된 좌표의 순서 역전을 발견했다. 이는 표기 연결 후보를 재검토할 근거이며 실제 치수가 틀렸다는 확정 판정이 아니다.

현재는 계산 모듈과 실제 도면 검증 러너에 연결했다. 제품의 3D 생성에 적용하지 않았으며 전체 자동 생성은 계속 0/3이다. 다음 작업은 치수선 기반 전체/부분 구간 제약 통합, 미결정 좌표 연결 근거 확보, 원본 이미지·라벨·계단 영역을 함께 옮기는 비균일 좌표 변환이다. 결과는 `docs/validation/2026-09-11-dimension-constraints-retest.json`에 기록했다. 외부 API 및 웹 배포 없이 진행했다.


## 치수선의 전체·부분 길이와 인접 표기 제약 통합

치수선 양 끝 보조선으로 확인한 구간을 `readMeasuredSpans`에서 공통 수집한다. 자동 공간 생성의 기존 축척 계산도 같은 수집 함수를 사용한다. 좌표 계산기는 이 구간과 인접 표기를 함께 받아 벽 중간의 치수 끝점까지 연결하고, 두 근거 사이의 모순을 검사한다. 치수선의 교차축 위치를 새 벽 좌표로 추가하지 않으며 단위 불명·낮은 신뢰도·경쟁 구간은 제외한다.

256개 테스트 및 빌드 통과. 실제 3페이지 × 3조건 재검증에서는 기존의 엄격한 치수선 근거에 맞는 구간이 0개였다. 따라서 이번 통합으로 실제 미결정 좌표 수나 완전 자동 생성률이 개선되지는 않았다(0/3 유지). 결과: `docs/validation/2026-09-11-combined-constraints-retest.json`. 다음은 이 도면들의 벽 옆 표기 연결과 축 좌표 정렬 근거를 개선하거나, 치수선이 있는 추가 실제 도면에서 통합 경로를 검증해야 한다. 비균일 3D 좌표 적용은 여전히 미구현이다. 외부 API와 웹 배포 없이 진행했다.
