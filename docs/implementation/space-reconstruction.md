# 실제 도면 공간 복원 — 실행 계획 및 1차 구현

## 목표

항목별 사용자 확인이나 외부 인식 API 없이 도면 구조를 복원한다. 문·창문의 빈 구간을 가짜 벽으로 닫지 않는다. 실제 도면 완성률로 평가하며 배포는 별도 시점에 진행한다.

## 구현 완료: 연결 구조와 짧은 벽

- 실제 교차점에서 선분을 분리해 T자·십자 접합을 같은 좌표로 연결한다.
- 평면 그래프의 방향별 면 순회로 공유 벽이 있는 여러 실의 외곽과 내부 벽을 분리한다.
- 반대 방향으로 중복된 선은 제거한다. 부분적으로 겹치는 선과 순환 영역 사이의 불명확한 연결은 보류한다.
- 오목한 외곽과 중정을 가로지르는 외부 선 차단을 유지한다.
- 짧은 선을 일괄 제거하지 않고, 양끝이 연결되고 긴 구조선 네트워크에 속한 연결부는 유지한다. 인식된 문자 상자 내부의 선과 독립 글자 고리는 제외한다.
- 교차점에서 벽이 분할되더라도 원래 전체 길이 치수로 축척을 검증한다.

검증: 160개 테스트 통과, 빌드 성공. 공유 방·교차 접합·사선 분할·순서/방향 불변성·짧은 꺾임·축척 연결을 검사했다. 입력 후보의 사선 연결 처리는 지원하지만 래스터에서 사선을 검출하는 기능은 아직 없다. 200개 벽 상한을 넘는 분할은 보류한다.

## 실제 도면 재검증

동일한 ANCA 1페이지와 Espacio 2페이지, 각 3개 밝기 조건으로 다시 실행했다. 전체 생성은 여전히 0/3이다. Espacio 2의 구조 후보가 조건별 24/30/31개에서 25/31/34개로 바뀌었다. 후보 증가를 정확도 향상이나 성공으로 집계하지 않는다. 세부 결과는 `../validation/2026-09-11-topology-retest.json`에 있다.

## 다음 구현 순서

1. **개구부를 가진 경계 모델**: 실제 벽과 바닥 경계 연결부를 별도 타입으로 정의한다. 문·창문의 양끝, 유형, 도면 근거를 보존한다. 저장/불러오기·2D/3D·수정·Undo까지 연결한 뒤 인식 결과를 적용한다. 문을 통과할 수 있는 경계가 벽으로 렌더되거나 작품 설치면이 되면 실패다.
2. **이중선 및 선 후보 유실 처리**: 평행한 두 선을 거리만으로 합치지 않고 공통 구간·끝단·연결 관계를 검사한다. 가까운 복도 벽 두 개가 합쳐지면 실패다. 100개 후보 상한에서 짧은 구조 근거를 잃지 않도록 구조용/치수용 후보를 구분한다.
3. **문·창문 구조 근거 결합**: 개구부 양끝과 문짝/회전호/창선 등 기하 근거를 검사한다. 문자 위치만으로 물리적 폭을 만들지 않는다. 닫히지 않은 경계는 임의 보간하지 않는다.
4. **사선과 실제 정답 검증**: 사선 검출을 추가하고 좌표를 주석한 도면으로 벽·개구부 누락/오검출을 비교한다. 같은 3페이지 회귀 검사와 별도 도면 검증을 병행한다.

전체 단위 상속 및 회전 숫자는 치수 인식 트랙과 연동해야 하며, 공간 복원만으로 해결된 것으로 간주하지 않는다. 웹 배포는 이번 작업에서 하지 않는다.


## 개구부 경계 모델 및 자동 연결

문/창문을 실제 벽과 분리된 개구부로 저장하고 벽 끝점에 연결했다. 바닥 경계에만 가상 연결을 사용하며, 작품 설치면이나 3D 벽 메시로 생성하지 않는다. 2D/3D는 개구부를 점선으로 표시한다. 벽 수정 시 연결점을 따라가고, 삭제 시 해당 개구부를 제거하며 Undo/Redo로 복원한다. JSON 검증은 잘못된 참조/중복 ID/0 길이를 거부한다.

문·창문 표기와 같은 선상에서 서로 마주 보는 벽 끝점의 틈이 유일하게 연결될 때 자동 후보를 생성한다. OCR 90 미만, 여러 후보 충돌, 다른 선과 교차, 검증 범위 밖 폭은 보류한다. 폭은 문자 상자가 아니라 실제 벽 끝점으로 계산한다. 문짝 회전호·창틀·상인방/창턱 높이는 아직 추론하지 않는다. 문·창문 후보 유형과 위치도 3회 자체 검증 결과 비교에 포함했다.

169개 자동 테스트와 빌드 통과. 별도 로컬 포트의 브라우저에서 개구부 프로젝트 불러오기, 3D의 열린 출입구와 이어진 바닥, 평면 전환 및 오류 없음 확인. 공개 도면 3페이지 재검증은 여전히 완전 생성 0/3이다. `docs/validation/2026-09-11-openings-retest.json` 참조. 웹 배포하지 않음. 전체 다섯 목표는 계속 미완료 상태이며 다음 구현은 이중선·선 후보 유실 처리다.


## 후보 보존·이중선·전체 단위 선언

자동 분석은 최대 500개 선 후보를 보존하고 수동 도구는 기존 100개 기본값을 유지한다. 저장 검증도 500개까지 허용한다. 양끝을 잇는 선이 유일하게 확인되는 평행 이중선만 중심선 후보로 변환한다. 끝이 열린 평행선, 복도 폭의 간격, 경쟁하는 끝단 근거는 합치지 않는다. 이 판정은 구조 후보 판정이며 길쭉한 가구/창틀의 의미 분류까지 해결한 것은 아니다.

페이지 전체의 명시적 단위 선언(예: All measurements in cm, 단위: mm)을 표기로 보존하고 단위 없는 숫자의 자동 축척 계산에 적용한다. 원래 숫자와 개별 명시 단위를 유지하며 서로 충돌하는 전체 단위, 낮은 OCR 점수, 특정 객체 치수 문구는 기본 단위로 사용하지 않는다. Espacio 실제 두 페이지에서 cm 선언 보존을 확인했다.

179개 테스트와 빌드 통과. 동일 공개 도면 재검증은 전체 생성 0/3으로 유지되며, 일부 입력은 500개 상한에도 도달했다. 후보 수 증가는 정확도 개선으로 집계하지 않는다. `docs/validation/2026-09-11-paired-units-retest.json`에 결과를 보존했다. 외부 API와 웹 배포는 사용하지 않았다. 다음은 세로/회전 숫자와 구조선 근거 보완이다.


## 전체/부분 치수 연결 및 두꺼운 선 교차점 보정

연속된 여러 벽의 전체 치수, 하나의 벽 중간에 놓인 부분 치수를 치수선 및 양끝 보조선으로 연결한다. 실제 벽이 없는 구간은 연결하지 않는다. 부분 치수가 전체 구간을 빈틈없이 구성할 때 합계를 대조하고 2%를 넘는 불일치를 보류한다. 겹치거나 경쟁하는 부분 치수는 합산하지 않는다. 자동 공간 생성과 축척 검증에 통합했다.

두꺼운 래스터 선 끝의 외곽 픽셀과 수직 벽 중심선 사이 오프셋을 선 두께와 상한 12px로 보정한다. 수평·수직 방향을 유지하고, 경쟁하는 교차점/문 크기의 틈/선분 붕괴는 보정하지 않는다. 고정 4px 근접 연결 전에 처리한다.

193개 테스트와 빌드 통과. 동일한 공개 도면 재검증은 전체 생성 0/3이다. Espacio에서 구조 후보가 늘었지만 정답 구조 재현율이나 성공률 향상으로 집계하지 않는다. 결과는 `docs/validation/2026-09-11-dimension-spans-retest.json`에 보존했다. 다음은 외곽 후보와 가구·범례 선 분리 및 사선 검출이다. 전체 다섯 목표는 미완료이며 외부 API/웹 배포는 사용하지 않는다.


## 래스터 벽 띠 병합·회전 시설 표기·계단 검출 범위

두꺼운 벽이 여러 평행 띠로 분리되었을 때 원본 이진 픽셀의 97% 이상이 채워진 구간만 병합한다. 16px 두께 상한, 길이 90% 이상 겹침과 끝점 차이 제한을 적용하며 빈 간격이 있는 평행선은 유지한다. 회전 OCR에서 얻은 90점 이상 시설 표기도 보존한다.

계단 표기 주변의 규칙적인 반복 단 선 3개 이상으로 검출 범위를 만든다. 문자 상자를 면적으로 사용하지 않는다. 최소 두 분석 조건이 일치하고 나머지 검출 결과에 모순이 없어야 분석 화면과 평면도에 표시한다. JSON 저장/불러오기에서 선·표기 참조와 범위를 검증한다. 현재는 검출한 단 선의 범위 표시이며 계단 전체 외곽 복원, 3D 계단 모델, 작품 배치 차단은 아직 구현하지 않았다.

202개 자동 테스트 및 빌드 통과. 실제 Espacio 1페이지에서 출입문·창문·계단 표기를 얻었고 계단 영역 1개가 세 조건에서 일치했다. 공개 도면 3페이지의 전체 공간 자동 생성은 여전히 0/3이다. 결과는 `docs/validation/2026-09-11-stair-regions-retest.json`에 저장했다. 로컬 Node Canvas/OCR 검증이며 이번 표시 변경의 브라우저 검증은 별도다. 외부 인식 API 및 웹 배포 없이 구현했다.

다음 우선순위는 실제 도면 외곽의 누락 선 복원과 가구·계단 단 선의 벽 오분류 제거다. 이후 사선, 완전한 설치 제외 영역 및 자동 페이지 선택을 검증한다. M0와 다섯 세부 목표는 계속 미완료다.


## 계단 단 선의 벽 오분류 제거

계단 표기와 규칙적 단 선의 결합 근거가 있을 때, 가운데 단 선을 이중벽 병합·구조 그래프 구성 전에 벽 후보에서 제외한다. 양쪽 끝 선은 계단실 벽일 수 있어 유지하며 다른 선으로 같은 방향으로 이어지는 단 선도 제거하지 않는다. 원본 분석 선은 보존한다. 자동 공간 채택에는 기존의 여러 검출 조건 간 일치 검증이 계속 적용된다.

204개 테스트와 빌드를 통과했다. 실제 Espacio 1페이지에서 세 조건 모두 가운데 계단 선 1개가 벽 후보에서 빠져 후보 수가 32/34/39 → 31/33/38로 변했다. 다른 두 페이지는 변하지 않았다. 전체 공간 생성은 여전히 0/3이며 후보 수 감소를 전체 구조 정확도로 집계하지 않는다. 결과는 `docs/validation/2026-09-11-stair-tread-retest.json`에 보존했다. 외부 API·웹 배포 없이 진행했으며 다음 핵심 과제는 외곽 누락 선과 사선 검출이다.


## 사선 래스터 검출과 모서리 연결

수평·수직 띠를 제거한 잉크의 연결 성분에 주축 직선을 맞춰 사선 후보를 검출한다. 길이·폭·횡방향 편차·연속 구간 조건을 함께 확인하며 서로 떨어진 성분을 합치지 않는다. 꺾인 선 묶음은 직선으로 바꾸지 않는다. 이것은 사선 후보 검출이며 가구와 벽의 의미 구분이나 서로 연결된 복잡한 사선망 전체의 분해까지 해결한 것은 아니다.

두꺼운 구조 후보의 사선 교차점도 기존 12px 이내 두께 기반 오차 범위에서 연결한다. 얕은 교차각, 경쟁하는 교차점, 큰 틈은 보정하지 않는다. 얇은 치수 보조선은 구조 모서리 보정에 참여시키지 않는다. 사선 후보가 수직 띠 병합에 들어가는 경우도 차단했다.

209개 테스트와 빌드 통과. 양·음 방향과 수평/수직에 가까운 각도, 실제 틈 보존, 꺾인 성분 거부를 검증했다. 사선 외벽을 포함한 합성 래스터 방에서 벽 검출→모서리 연결→독립 치수 근거 2개→축척 있는 닫힌 공간 생성까지 통과했다. 합성 사례는 실제 도면 완료율에 포함하지 않는다.

공개 도면 재검증 결과는 `docs/validation/2026-09-11-diagonal-retest.json`이며 전체 자동 생성은 여전히 0/3이다. 새로 검출된 사선 후보의 실도면 정답 여부는 별도 주석 검증이 필요하다. 사용자가 체크하는 단계는 추가하지 않았다. 외부 API/웹 배포 없이 진행했다. 다음은 외곽 누락 구간의 원본 픽셀 근거와 복잡한 선망 분해다. 전체 목표는 미완료다.


## 연결 근거가 있는 얇은 구조선 보존

1~3px의 긴 선도 양끝이 서로 다른 두꺼운 구조 후보에 직접 연결될 때 유지한다. 얇은 선끼리의 연결은 채택 근거로 쓰지 않으며, 독립 구조 시작점도 두꺼운 선으로 제한한다. 문자 상자 내부 선 제거와 기존 연결 그래프 검증을 유지한다. 짧은 사선 래스터 조각이 재유입되지 않도록 긴 선에만 적용한다.

212개 테스트와 빌드 통과. 얇은 외곽선이 포함된 합성 공간의 전체 생성과 치수 적용을 검증했다. 실제 Espacio 1페이지 후보는 32/33/38→33/36/39, 2페이지는 37/37/45→40/39/52로 변했다. 전체 생성은 여전히 0/3이며 추가 후보의 정답 여부를 전체 정확도 개선으로 주장하지 않는다. 결과는 `docs/validation/2026-09-11-thin-boundary-retest.json`에 기록했다.

추가 원인 확인: Espacio 1페이지 아래쪽에는 2400px 좌표 기준 (123.43,1492.79)~(1263.43,1492.79)의 연속 얇은 선이 원본 검출 결과에 존재한다. 왼쪽 벽 중심 x≈132보다 바깥으로 연장되어 현재 4px 직접 연결 조건을 통과하지 못한다. 선 누락이 아니라 끝점 돌출 및 일부 구간의 두꺼운 평행 띠 중복 문제다. 다음에는 실제 교차점 기반 끝점 보정과 부분적으로 겹치는 띠의 픽셀 근거를 처리해야 한다. 외부 API 및 웹 배포 없이 진행했으며 전체 목표는 미완료다.


## 얇은 경계 끝점 돌출 보정

얇고 긴 선의 끝 12px 이내에 두꺼운 선과 실제 교차점이 유일하게 있을 때 돌출 부분만 줄인다. 선을 연장하지 않으며, 상대 선의 범위 밖 교차점, 여러 교차점, 얕은 교차각, 얇은 보조선은 근거로 사용하지 않는다. 원본 검출 선은 보존한다.

216개 테스트 및 빌드 통과. 돌출된 얇은 외곽이 포함된 합성 공간의 전체 생성과 축척 적용을 확인했다. 실제 Espacio 1페이지의 기준 조건(155)에서 아래쪽 긴 경계가 (132,1492.79)~(1261.71,1492.79)로 구조 후보에 복원됐다. 동일 구간의 두꺼운 평행 띠는 여전히 남아 있어 중복 해소가 필요하다. 전체 자동 생성은 0/3이며, 특정 경계 복원과 전체 정확도를 구분한다. 결과: `docs/validation/2026-09-11-thin-overrun-retest.json`.

다음은 원본 픽셀에 근거한 부분 평행 띠 병합이다. 외부 API 및 웹 배포 없이 진행했고 전체 목표는 미완료다.


## 부분 평행 띠의 원본 픽셀 기반 중복 제거

긴 선에 완전히 포함된 평행 조각은 두 띠 사이의 전체 해당 픽셀 구간이 97% 이상 채워지고 합친 폭이 16px 이내일 때만 제거한다. 긴 선의 실제 좌표와 두께는 유지한다. 같은 선상에서 치수 보조선이 벽보다 길어지는 경우는 제외하며, 빈 틈이 있거나 일부만 겹친 선도 유지한다.

두꺼운 조각에서 얻은 구조 지지는 `solidSupportThicknessPx`로 별도 보존하고 원본 좌표 변환 시 함께 환산한다. 구조 후보 구성에서만 이 근거를 사용하며 실제 벽 두께로 단정하지 않는다. JSON 검증에도 유한값·범위를 추가했다. 기존 래스터 공간 생성 회귀 테스트가 밝힌 보조선 오채택을 수정했다.

219개 테스트와 빌드 통과. 실제 Espacio 1페이지 기준 조건(155)에서 아래쪽 경계는 (132,1492.79)~(1261.71,1492.79) 한 선으로 유지되고 이전 두꺼운 평행 중복은 제거됐다. 전체 자동 생성은 여전히 0/3이다. 결과는 `docs/validation/2026-09-11-contained-bands-retest.json`이며 전체 목표는 미완료다. 다음은 남은 출입구 주변 경계와 외부 비구조 선의 분리다. 외부 API와 웹 배포 없이 진행했다.


## 창틀이 연결된 창문 개구부

창문 표기와 일치하는 벽 끝점 사이에 평행 창틀 한 선·양끝 짧은 연결선 두 개가 있을 때, 이 세 선을 설치 가능한 벽에서 제외하고 창문 개구부로 변환한다. 양쪽 실제 벽 끝점으로 폭을 정하며 문자 상자를 크기로 사용하지 않는다. 연결 깊이 30px 이내, 평행·폭 일치, 유일한 해석을 요구하고 다른 벽의 분기·교차, 불완전한 창틀, 낮은 OCR 점수, 중복 표기는 보류한다. 기존 개구부 저장·3D/평면 표시·다중 조건 검증을 사용한다.

222개 테스트와 빌드 통과. 창틀 포함 합성 공간에서 창문 개구부 및 전체 공간 생성, JSON 검증을 통과했다. 실제 Espacio 1페이지는 기준 조건155에서 창문 1개를 검출했지만 125/190에서는 미확보했다. 따라서 실도면에서 안정적인 자동 채택을 달성한 것으로 간주하지 않는다. 전체 생성은 여전히 0/3이다. 결과는 `docs/validation/2026-09-11-framed-windows-retest.json`에 보존했다.

다음은 조건별 창틀 선 누락과 정문 주변의 남은 경계 복원이다. 다섯 목표는 계속 미완료이며 외부 API와 웹 배포 없이 진행했다.


## 창틀/빈 틈 표현 간 개구부 검출 안정화

창틀 기반 변환과 빈 틈 기반 변환을 공통 개구부 해석 함수로 묶었다. 원본 선 조건에 따라 창틀이 사라져도 같은 실제 벽 끝점의 창문으로 해석되는지 검증한다. 창틀 판정의 평행/정렬 오차는 2px까지 허용하며 더 크게 어긋난 경계는 보류한다. 실제 Espacio 1페이지의 190 조건은 약 1.7px 차이로 기존 1px 조건에 막혀 있었다.

224개 테스트와 빌드 통과. 실제 Espacio 1페이지의 동일한 뒤쪽 창문이 125 조건에서는 빈 틈, 155/190에서는 창틀로 인식되어 세 조건 모두 같은 개구부 후보를 얻었다. 시작점 차이는 최대 약 1.7px, 끝점 차이는 약 0.86px이다. 이는 해당 개구부의 조건 간 일치이며 전시장 전체 완료를 뜻하지 않는다. 전체 공간 생성은 여전히 0/3이다. 결과는 `docs/validation/2026-09-11-window-stability-retest.json`에 보존했다.

다음은 정문 주변의 누락된 구조선과 다른 개구부의 해석이다. 외부 API 및 웹 배포는 사용하지 않았으며 전체 목표는 미완료다.


## 실도면 구조 구간 정답 및 단계별 누락 측정

Espacio 1페이지 원본을 직접 확인해 선택한 벽 구간 6개를 `docs/validation/espacio-selected-structure.json`에 기록했다. 원본 SHA-256·페이지·크기 일치 시에만 점수를 계산한다. 4px 위치 오차 내 평행 후보의 실제 겹친 구간 합집합으로 길이 커버리지를 계산하며 중복 선을 중복 집계하지 않는다. raw(선 검출)와 selected(구조 선택)를 분리한다. 95% 이상을 구간 검출로 판정한다. 전체 도면 정답이나 실제 mm 치수 정확도가 아닌 선택 구간의 픽셀 기준 측정이다.

226개 테스트 및 빌드 통과. 세 조건 모두 정문 위 수직/수평 연결 2개는 raw 100%, selected 0%였다. 나머지 선택 구간 4개는 모두 95% 이상 유지됐다. 즉 이 구간 실패는 원본 선 누락이 아니라 얇은 연결선 묶음의 구조 선택 문제다. 전체 공간 생성은 계속 0/3이다. 결과는 `docs/validation/2026-09-11-structure-coverage-retest.json`에 보존했다.

다음 구현은 두꺼운 외벽부터 문설주까지 이어지는 얇은 연결선 묶음을 처리하는 것이다. 문설주/개구부의 의미 근거를 함께 사용해야 하며 일반 치수 보조선 묶음을 벽으로 승격하지 않아야 한다. 외부 API와 웹 배포 없이 진행했으며 다섯 목표는 미완료다.


## 모서리 출입구 및 연결선 묶음 보존

한쪽 벽은 문 방향과 직각이고 다른 벽은 문 방향과 평행한 모서리 출입구를, 확실한 문 표기와 실제 벽 끝점으로 검출한다. 문 구간을 실제 선이 막거나 해석이 경쟁하면 보류한다. 문 끝점에 연결된 짧은 선 묶음은 두꺼운 구조 시작점까지 이어지는 유일한 최대 4개 선 경로가 있을 때 유지한다. 근거 없는 얇은 선끼리의 연결은 여전히 채택하지 않는다.

진단 수정: 정문 연결선은 조건155에서 두께 자체도 3px 이상이었다. 직접 원인은 짧은 종단 선을 제거하는 규칙과 모서리 문 형태를 지원하지 않는 규칙의 결합이었다. 조건에 따라 더 얇은 선도 있으므로 출입구 의미와 구조 연결을 함께 사용한다.

230개 테스트 및 빌드 통과. 모서리 문이 포함된 합성 공간의 개구부 생성·전체 구조 생성·JSON 검증을 통과했다. 실제 Espacio 1페이지 정문 위 두 구간은 조건125/155에서 기존 selected 0%→약98.5~100%로 복원됐고 정문 개구부도 검출됐다. 이 두 조건에서 선택한 구조 구간 6개 모두 95% 이상이다. 조건190의 정문 연결선은 아직 누락된다. 전체 생성은 계속 0/3이며 전체 정답 복원으로 주장하지 않는다. 결과: `docs/validation/2026-09-11-corner-doors-retest.json`.

다음은 나머지 조건의 정문 형상 차이와 계단 쪽 열린 경계·내부 개구부 처리다. 외부 API·웹 배포 없이 진행했으며 전체 목표는 미완료다.


## 계단 검출 범위의 3D 표시와 작품 겹침 경고

축척이 확정된 도면의 계단 검출 범위를 실제 좌표로 변환해 3D 바닥에 표시한다. 작품의 회전·폭·깊이·프레임과 벽 앞뒤 설치면을 반영한 평면 점유 다각형으로 겹침을 검사한다. 겹치면 3D 작품 위와 속성창에 자동 경고하며 이동 후 다시 계산한다. 벽 치수 변경 미리보기의 경고에도 포함한다. 축척 미확정 또는 제외된 계단 표기는 실제 좌표 제한으로 사용하지 않는다.

이는 검출된 범위의 표시와 배치 경고다. 저장/배치를 강제 차단하지 않으며 계단 전체 형상·높이·법정 이격거리를 추정하지 않는다. 에어컨·소화전 등 다른 설비의 실제 영역 자동 인식은 여전히 미완료다.

233개 테스트 및 빌드 통과. 로컬 브라우저에서 검증 프로젝트 불러오기, 3D 계단 영역 및 작품 겹침 경고 표시, 작품 위치 1400→2800mm 이동 후 3D/속성창 경고 해제, 브라우저 오류 없음을 확인했다. 단위 테스트는 회전 다각형 판정, 벽 뒷면, 이동, 미확정 축척과 제외 표기를 검증했다. 이번 변경은 실도면 인식기를 바꾸지 않았으며 마지막 실제 전체 생성 결과는 0/3이다. 외부 API와 웹 배포는 사용하지 않았다. 전체 목표는 계속 미완료다.


## 여러 페이지 PDF 자동 분석·선택

다중 페이지 PDF의 기본값을 첫 페이지 수동 선택에서 자동 선택으로 바꿨다. 페이지를 순서대로 렌더·로컬 분석하며 자체 공간 검증을 통과한 첫 페이지에서 멈춘다. 실패한 페이지는 건너뛰고 계속한다. 검증된 공간이 없으면 구조 후보와 숫자 근거 점수가 가장 높은 페이지를 미확정 후보로 표시한다. 동점은 앞 페이지를 유지한다. 이 점수는 평면도 여부의 검증된 분류 정확도가 아니다. 처리한 페이지 수·선택 페이지·실패 건수를 안내한다.

전체 페이지를 동시에 보관하지 않고 가장 좋은 결과만 유지해 메모리를 제한한다. 취소/닫기 및 오래된 요청 결과의 반영을 차단한다. 모든 인식이 실패해도 읽을 수 있었던 원본을 보존하며 즉시 같은 실패 분석을 무한 반복하지 않는다. 단일 페이지 흐름과 수동 페이지 변경은 유지한다.

237개 테스트 및 빌드 통과. 실제 Espacio 두 페이지 PDF를 브라우저에서 업로드한 뒤 선택 조작 없이 2페이지까지 분석하고 2페이지를 미확정 후보로 표시하는 것을 확인했다. 공간 자체 검증 실패 상태를 유지했고 브라우저 오류가 없었다. 단위 테스트는 페이지 실패 이후 계속 처리, 첫 검증 성공 시 중단, 후보/동점 처리, 전체 실패와 원본 보존, 분석 중 취소를 검증한다.

이 변경은 다중 페이지 선택 자동화의 첫 단계다. 여러 층 병합, 자동 영역 분리와 페이지 방향 정규화, 최적 페이지 분류 정확도는 아직 미완료다. 마지막 실도면 전체 공간 생성은 0/3이며 다섯 목표는 계속 진행 중이다. 외부 API와 웹 배포 없이 진행했다.


## 계단 통로의 바닥 경계 처리

계단 반복 단 선의 검출 범위, 양쪽 연결선, 서로 마주 보는 실제 벽 끝점이 일치할 때 계단 통로를 검출한다. 계단 쪽 선들은 원본 분석에 보존하면서 전시장 바닥 경계를 만드는 벽에서는 제외한다. 계단의 폭을 문자 상자로 만들지 않고 실제 벽 끝점으로 통로 폭을 정한다. 범위 불일치·연결선 누락·경쟁하는 계단 해석은 보류한다.

개구부 모델에 stair-access를 추가해 바닥 경계만 이어주고 물리적인 벽이나 작품 설치면을 만들지 않는다. JSON 검증과 2D/3D 명칭에도 반영했다. 통로 바깥 계단 전체 형상이나 층간 연결을 자동 모델링한 것은 아니다.

240개 테스트와 빌드 통과. 합성 도면에서는 계단 통로 포함 전체 공간 생성·축척 적용·JSON 검증을 통과했다. 실제 Espacio 1페이지는 세 조건 모두 계단 통로 후보를 얻었다. 정문과 뒤쪽 창문 후보도 함께 처리하지만 전체 공간 생성은 여전히 0/3이다. 결과: `docs/validation/2026-09-11-stair-access-retest.json`. 다음은 남은 내부 선 접합과 개구부, 치수 근거 검증이다. 외부 API 및 웹 배포 없이 진행했으며 다섯 목표는 미완료다.


## T자 접합의 래스터 오차 보정

벽 끝점이 다른 두꺼운 선의 내부 교차점에서 조금 어긋난 경우 양쪽 방향을 유지한 채 유일한 교차점으로 보정한다. 12px 이내 두께 기반 상한, 충분한 교차각, 상대 선 내부 조건을 적용한다. 경쟁 교차점·큰 틈·상대 선 밖 연장·얇은 문자 선은 보정하지 않는다. 이후 기존 평면 그래프 단계가 T자 접점을 분할한다.

244개 테스트 및 빌드 통과. 오차가 있는 내부 가벽의 전체 공간 생성과 치수 보존을 검증했다. 실제 Espacio 1페이지에서 조건125/155가 닫힌 외곽·내부 벽 그래프 검증을 통과했다. 자동 생성의 실패 원인은 이 두 조건에서 구조 불확정→독립 치수/축척 근거 부족으로 바뀌었다. 조건190과 다른 두 페이지는 여전히 구조 단계에서 실패한다.

검증 러너에 topologyResolved를 추가해 닫힌 구조와 치수 포함 최종 생성을 분리한다. 구조 검증 통과는 실제 정답 형상을 완전히 복원했다는 의미가 아니다. 전체 자동 생성은 계속 0/3이다. 결과: `docs/validation/2026-09-11-raster-junction-retest.json`. 다음은 치수선 없이 벽 옆에 기입된 숫자의 연결 근거와 도면 축척 일관성 검증이다. 외부 API 및 웹 배포 없이 진행했으며 다섯 목표는 미완료다.


## 치수선 없는 벽 옆 숫자와 비례 일관성

독립 치수선 근거가 부족할 때 숫자 방향·벽 중심 구간·거리로 유일하게 연결되는 인접 표기를 검사한다. 세 개 이상의 서로 다른 벽과 수평/수직 두 방향의 근거, 단위, OCR 신뢰도, 충돌 부재, 2% 이내 비율 일치를 모두 만족해야 하나의 축척으로 적용한다. 높이/두께 문구·중복/경쟁 벽·불확실한 값은 사용하지 않는다.

248개 테스트 및 빌드 통과. 치수선 없는 합성 비례 도면에서 인접 표기 세 개를 통한 공간 생성·JSON 검증을 통과했고, 불일치 표기에서는 자동 적용을 보류한다. 실제 Espacio 1페이지 기준 조건155에서 14개 인접 표기 후보를 연결했다. 예를 들어 4280mm/600px≈7.133mm/px, 1900mm/336.86px≈5.640mm/px이며 세로 후보들도 서로 다르다. 따라서 현재 연결 근거로는 단일 축척을 적용할 수 없다. 인접 표기 연결은 추론이므로 이를 모든 도면이 비례 없이 그려졌다는 확정 증거로 일반화하지 않는다.

실제 1페이지는 두 조건에서 구조 검증을 통과하지만 치수 비율 충돌로 전체 생성을 보류한다. 전체 성공은 계속 0/3이다. 결과: `docs/validation/2026-09-11-wall-annotations-retest.json`. 다음 주요 구현은 개별 치수 제약에 따른 좌표 재구성과 미기입 구간의 일관성 검증이다. 이미지의 단일 mmPerPixel만 바꾸는 방식으로는 해결되지 않는다. 외부 API와 웹 배포 없이 진행했으며 전체 목표는 미완료다.


## 개별 치수 제약의 상대 좌표 계산

`dimensionConstraints.ts`에 벽별 길이로 x/z 상대 좌표를 계산하는 그래프 검증을 추가했다. 같은 축 좌표는 소수점 여섯 자리로 정규화하며, 떨어진 좌표를 임의로 합치지 않는다. 연결된 치수마다 길이를 고정하고 전체/부분 치수의 순환 모순(0.001mm 초과), 좌표 순서 역전, 부적합한 입력을 검출한다. 연결되지 않은 성분은 각각 별도 원점을 가지며 미결정 상대 위치 수를 반환한다. 픽셀 비율로 누락 치수를 보간하지 않는다.

253개 테스트 및 빌드 통과. 실제 3페이지를 3가지 조건으로 재실행했다. Espacio 1페이지 조건155는 수평 15개·수직 18개의 연결 성분으로 남아 전체 좌표가 미결정이다. 단일 축척 불일치가 개별 치수 자체의 모순을 의미하지는 않으며, 이 페이지에서는 현재 연결된 제약 안의 순환 모순은 없었다. Espacio 2페이지 조건155/190에서는 연결된 좌표의 순서 역전을 발견했다. 이는 표기 연결 후보를 재검토할 근거이며 실제 치수가 틀렸다는 확정 판정이 아니다.

현재는 계산 모듈과 실제 도면 검증 러너에 연결했다. 제품의 3D 생성에 적용하지 않았으며 전체 자동 생성은 계속 0/3이다. 다음 작업은 치수선 기반 전체/부분 구간 제약 통합, 미결정 좌표 연결 근거 확보, 원본 이미지·라벨·계단 영역을 함께 옮기는 비균일 좌표 변환이다. 결과는 `docs/validation/2026-09-11-dimension-constraints-retest.json`에 기록했다. 외부 API 및 웹 배포 없이 진행했다.


## 치수선의 전체·부분 길이와 인접 표기 제약 통합

치수선 양 끝 보조선으로 확인한 구간을 `readMeasuredSpans`에서 공통 수집한다. 자동 공간 생성의 기존 축척 계산도 같은 수집 함수를 사용한다. 좌표 계산기는 이 구간과 인접 표기를 함께 받아 벽 중간의 치수 끝점까지 연결하고, 두 근거 사이의 모순을 검사한다. 치수선의 교차축 위치를 새 벽 좌표로 추가하지 않으며 단위 불명·낮은 신뢰도·경쟁 구간은 제외한다.

256개 테스트 및 빌드 통과. 실제 3페이지 × 3조건 재검증에서는 기존의 엄격한 치수선 근거에 맞는 구간이 0개였다. 따라서 이번 통합으로 실제 미결정 좌표 수나 완전 자동 생성률이 개선되지는 않았다(0/3 유지). 결과: `docs/validation/2026-09-11-combined-constraints-retest.json`. 다음은 이 도면들의 벽 옆 표기 연결과 축 좌표 정렬 근거를 개선하거나, 치수선이 있는 추가 실제 도면에서 통합 경로를 검증해야 한다. 비균일 3D 좌표 적용은 여전히 미구현이다. 외부 API와 웹 배포 없이 진행했다.


## T자 접점으로 나뉜 벽 옆 치수 구간

실제 Espacio 1페이지의 하단 386cm 표기는 긴 하단 벽 전체가 아니라 내부 가벽 접점까지의 구간이다. 인접 표기 후보에 실제 수직 벽 끝점이 만나는 구간을 추가했다. 전체 벽과 부분 구간을 함께 검사하고 둘 다 가능한 경우 보류한다. 떨어진 가벽을 임의로 연장하지 않는다. 표기의 from/to를 좌표 계산기에 전달해 부분 길이를 전체 벽 길이로 사용하는 오류를 방지한다.

259개 테스트와 빌드 통과. 원본에서 직접 확인한 한 구간을 `espacio-selected-annotations.json`에 기록하고 파일 해시·페이지·해상도와 4px 허용 오차로 검증한다. 실제 조건125/155에서 3860mm를 x132→696/695.14에 연결해 통과했고 190은 실패했다. 다른 표기 중 전체/부분 경쟁이 생긴 후보는 보류되어 조건155의 연결 수는 14→13이며 미결정 좌표 수는 줄지 않았다. 이를 전체 정확도 상승으로 주장하지 않는다. 전체 자동 생성은 0/3 유지다.

결과: `docs/validation/2026-09-11-junction-annotations-retest.json`. 다음은 경쟁 표기의 실제 대상 판정과 숫자 누락, 비균일 좌표 변환 연결이다. 웹 배포 없이 커밋·푸시하며 전체 목표는 미완료다.


## 구역별 확대 숫자 OCR

전체 페이지에서 누락되던 숫자를 보완하기 위해 서로 겹치는 네 구역을 0°/90°로 확대 인식한다. 기존 회전 인식 후 로컬 OCR로 순차 실행하며 새 숫자만 병합한다. crop·회전·확대 좌표를 원본 이미지 위치로 되돌리고, 빈 구역은 정상 처리하며 취소 시 다음 구역을 시작하지 않는다. 이미 확보한 PDF 문자 근거는 재사용한다. 기존 숫자 충돌은 다수결로 해소하지 않는다.

263개 테스트와 빌드 통과. 실제 Espacio 1페이지에서 110과 세로125를 신뢰도96으로 추가 복구했다. 기존에 직접 확인한 세로 숫자 7개 기준 5/7→6/7이며 60/09 충돌은 유지된다. 이는 전체 페이지 정확도나 공간 완성을 의미하지 않는다. 실제 3페이지 완전 자동 생성은 0/3이다. Node 검증 러너의 페이지 처리 시간은 Espacio 1 약7.2초, 2 약7.0초였으며 브라우저 성능과 동일시하지 않는다.

로컬 브라우저에 실제 Espacio 1페이지 이미지를 업로드해 숫자24개·구조선452개의 자동 분석 완료와 콘솔 오류 없음을 확인했다. 검증 대화상자는 취소해 기존 프로젝트를 보존했다. 결과: `docs/validation/2026-09-11-region-ocr-retest.json`. 웹 배포 없이 커밋·푸시하며 전체 목표는 미완료다.


## 개구부 양쪽 평행 벽의 축 연결

검출된 개구부의 양 끝에 각각 하나의 평행 벽이 바깥 방향으로 이어지는 경우, 2px 이내 축 차이를 같은 축이라는 계산 근거로 사용한다. 모서리 문·사선·경쟁 벽·큰 차이는 제외한다. 좌표 계산기가 이 근거로 치수 연결 성분을 합치며 연쇄 보정도 총2px 이내로 제한한다. 양수 치수 구간을 없애는 보정은 충돌로 처리한다. 근거와 보정 전 좌표를 결과에 남긴다.

266개 테스트와 빌드 통과. 실제 Espacio 1페이지의 뒤쪽 창문 양쪽 x132.86/x132를 같은 축으로 연결했다. 세 조건 모두 하나의 수평 좌표 연결 성분을 줄였으며 조건155의 성분 수는 x14/z17이다. 이는 표기 치수들 사이의 연결 개선이며 누락 길이 생성이나 전체 공간 완성을 의미하지 않는다. 현재 좌표 계산 모듈과 검증 러너에 반영했고, 비균일 3D 적용은 미구현이다. 전체 자동 생성 0/3 유지.

결과: `docs/validation/2026-09-11-opening-axes-retest.json`. 다음은 남은 개구부 치수 연결과 미기입 구간 처리, 원본 이미지·시설물 좌표 변환이다. 웹 배포 없이 커밋·푸시하며 전체 목표는 미완료다.


## 출입구·창문 폭 표기 연결

검출된 문/창문 개구부를 인접 숫자의 후보 구간으로 포함한다. 주변 벽과 경쟁하는 숫자는 제외하고, 같은 개구부의 서로 다른 폭 표기는 모두 보존하여 충돌을 검사한다. 좌표 계산기에 실제 개구부 양 끝과 표기 길이를 전달한다. 자동 공간 생성에도 개구부 표기 폭과 축척에서 계산한 폭이 2% 이내인지 확인하는 검증을 연결했다. 계단 통로의 폭으로는 전용하지 않는다.

269개 테스트 및 빌드 통과. 실제 Espacio 1페이지의 전면 출입구 96cm를 조건125/155에서 960mm로 연결했다. 조건155의 원본 구간은 y473.30→648.22이며, 190에서는 개구부 검출 실패로 연결하지 않는다. 이는 원본 표기 연결이며 현장 실측 검증이 아니다. 좌표 계산에는 중복 표기를 제거하기 전 모든 근거를 전달해 숨겨졌던 상충 표기도 검출한다. 전체 자동 생성은 계속 0/3이다.

결과: `docs/validation/2026-09-11-opening-dimensions-retest.json`. 다음은 비균일 좌표 변환과 미기입 구간 처리이며 웹 배포 없이 커밋·푸시했다. 전체 목표는 미완료다.


## 천장 높이 표기를 벽 높이 초안에 반영

페이지 전체의 명시적인 Ceiling height/천장 높이/층고 표기를 단위와 함께 읽는다. 객체 높이·특정 방 한정 문구·복수 높이·낮은 신뢰도·숫자 충돌은 일괄 높이로 사용하지 않는다. 단위 없는 층고는 명확한 페이지 단위 선언이 있을 때만 해석하며, 층고 숫자가 평면 길이로 쓰이지 않도록 높이로 분류한다.

273개 테스트 및 빌드 통과. 실제 Espacio 두 페이지에서 Ceiling height: 275cm를 2750mm로 읽었고 세 분석 조건의 벽 후보 높이에 반영됐다. 자동 생성 가능한 테스트 도면에서는 JSON 저장/복원 후에도 2750mm가 보존된다. 천장 높이는 개별 가벽의 실측 높이가 아니므로 벽 메모와 생성 안내에 높이 초안임을 명시한다. 벽 두께는 계속 임시값이다. 실제 전체 자동 생성은 구조/치수 문제로 0/3 유지다.

결과: `docs/validation/2026-09-11-ceiling-height-retest.json`. 웹 배포 없이 커밋·푸시하며 전체 목표는 미완료다.


## 치수 좌표의 구간별 변환 기반

완전히 결정되고 모순이 없는 x/z 치수 좌표에서 구간별 원본 픽셀↔공간 좌표 변환을 생성한다. 기준점 사이 위치 보간은 이미지·표시 위치를 옮기기 위한 것이며 새 실측 치수로 취급하지 않는다. 제약 범위 밖은 외삽하지 않는다. 사각형 영역은 양 모서리를 같은 변환에 통과시키고, 사선은 비율이 바뀌는 축 기준점에서 분할해 경로를 보존한다.

277개 테스트와 빌드 통과. 서로 다른 비율을 가진 치수 구간의 길이 보존·좌표 왕복·계단 형태의 사각형 변환·사선 경로 분할을 검증했다. 실제 도면 3페이지 재검증에서는 여전히 치수 좌표가 미결정/충돌 상태이므로 모든 조건에서 변환 생성이 보류됐다. `dimensionMapReady`를 검증 결과에 추가했다. 전체 자동 생성은 0/3이다.

이번 변경은 좌표 변환 모듈과 검증 러너까지이며 프로젝트 저장 형식·원본 이미지 렌더링·3D 적용에는 아직 연결하지 않았다. 다음은 이 변환을 저장하고 이미지/라벨/개구부/설비에 일관되게 적용하는 작업과 미결정 치수 처리다. 결과: `docs/validation/2026-09-11-dimension-map-retest.json`. 웹 배포 없이 커밋·푸시하며 전체 목표는 미완료다.


## 변환된 도면 레이어와 이미지 준비

`prepareMappedVenue`는 구간별 좌표 변환으로 벽을 나누고 개구부의 벽 끝점 참조를 새 조각에 연결한다. 숫자·설비 표기, 계단 영역과 검출 근거 선을 동일한 스칼라 도면 좌표로 옮겨 기존 프로젝트 형식을 사용할 수 있게 한다. 범위 밖 표기/선은 별도 결과로 반환하고 계단 영역 또는 근거 선이 누락되면 변환을 보류한다. 원본 페이지는 호출자가 보존해야 한다. 선 굵기는 표시용 보정이며 벽 두께 측정이 아니다.

`renderMappedPlan`은 같은 변환 셀로 원본 이미지를 다시 그린다. 렌더링 전에는 변환 이미지 URL을 붙이지 않는다. 기존 저장 한도와 취소/자원 해제를 적용했다. 테스트는 기존280개와 새 렌더링 테스트1개가 통과했고 빌드도 통과했다. 저장/복원한 변환 계단의 좌표가 x1000/z1200, 폭200/깊이800mm임을 검증했다. 렌더링 테스트는 캔버스 호출 검증이며 실제 브라우저 시각 검증은 아직 아니다.

자동 가져오기 흐름에는 아직 연결하지 않았다. 실제 도면 인식 알고리즘은 이번에 변경하지 않았으며 직전 실제 검증의 전체 생성 0/3 상태를 개선했다고 주장하지 않는다. 다음은 변환 가능한 분석 결과를 이 경로로 보내고 원본 보존·제외된 표기 처리·브라우저 렌더링을 검증하는 작업이다. 웹 배포 없이 커밋·푸시하며 전체 목표는 미완료다.


## 개별 치수 복원을 자동 가져오기와 3D에 연결

`prepareConstrainedVenue`가 닫힌 구조·개구부·전체/부분 치수·인접 숫자를 모아 좌표를 풀고 변환 레이어를 준비한다. `analyzePlan`은 기존 단일 축척 방식이 실패할 때 이 결과를 사용하며, 서로 다른 조건에서 두 개 이상 일치하고 다른 성공 결과와 충돌하지 않을 때만 이미지를 변환한다. 렌더링 실패/취소 시 적용하지 않는다. 원본 이미지·해상도·표기는 검증되는 `sourcePlan` 필드로 저장한다. 숫자/선/크기가 바뀌면 기존 변환 결과를 재사용하지 않는다.

미리보기는 변환된 이미지와 같은 좌표를 사용한다. 변환 전 원본을 펼쳐볼 수 있고 적용 후에도 원본 저장이 가능하다. 도면을 교체/제거하면 이전 원본 연결도 제거한다. 서로 다른 분석 결과 비교에 벽 높이·두께와 계단 설치 제외 영역도 포함했다.

실제 이미지 입력에서 치수선의 1px 끝 돌출이 벽 끝과 별개 좌표로 처리되는 문제를 발견했다. 보조선 검증을 유지하면서 유일한 벽 끝점이 1px 이내에 있을 때만 맞춘다. 치수 문자의 선과의 거리도 글자 크기에 따라 최대60px까지 허용하되 양 끝 보조선과 유일한 구간 조건은 유지한다.

285개 테스트와 빌드 통과. `node scripts/create-nonuniform-fixture.mjs`로 만든 검증 이미지(원본 비율800:600, 표기8000:9000)를 로컬 브라우저에서 업로드하고 자동 재분석→72㎡ 초안 생성→적용→8,000mm/9,000mm 치수가 표시되는 3D 벽·바닥까지 확인했다. 콘솔 오류는 없었으며 실행 취소로 기존 테스트 프로젝트를 복원했다. 이 합성 사례는 실제 도면 성공률에 합산하지 않는다.

실제 3페이지 재검증은 모든 조건에서 constrainedVenueReady=false, 완전 자동 생성0/3이다. 결과: `docs/validation/2026-09-11-constrained-venue-retest.json`. 비균일 치수의 자동 적용 경로는 연결됐지만 복잡한 실제 도면의 미결정 좌표·시설물 인식·입력 자동화·검증 확대는 남아 있다. 웹 배포 없이 커밋·푸시하며 전체 목표는 미완료다.


## 분리된 치수 묶음 사이의 배치 모순 검증

서로 연결되지 않은 치수 묶음도 원본 좌표의 순서를 공유한다. 인접 좌표의 순서를 유지하는 묶음별 위치 제약을 만들고 양의 순환이 생기면 배치 불가능으로 판정한다. 예를 들어 내부 구간2000mm가 이를 감싸는 구간1000mm 안에 들어가야 하는 경우, 각 묶음 내부에 숫자 모순이 없어도 전체 배치는 불가능하다. 보정된 위치값을 결과로 사용하거나 빈 구간의 길이를 생성하지 않는다. 0.001mm 수치 허용 오차를 사용한다.

287개 테스트 및 빌드 통과. 실제 Espacio 2페이지는 조건125에서 수평/수직, 155/190에서 수직 묶음 간 순서 모순이 확인됐다. 이는 현재 OCR/구간 연결 근거를 함께 배치할 수 없다는 뜻이며 원본 도면 자체가 틀렸다는 판정이 아니다. Espacio 1페이지와 ANCA에서는 이 종류의 모순은 검출되지 않았다. 그 부재가 좌표 결정이나 정확도 검증을 의미하지는 않는다.

결과: `docs/validation/2026-09-11-dimension-order-retest.json`. 전체 실제 도면 자동 생성0/3 유지. 다음 개선에서는 모순을 만드는 표기/구간 연결을 재판정해야 한다. 웹 배포 없이 커밋·푸시하며 전체 목표는 미완료다.


## 양쪽 벽면 숫자의 가림 관계로 오연결 수정

Espacio 2페이지의 영상 설치 공간 왼쪽에서 175/180 표기가 하나의 벽선에 연결되던 오류를 원본과 대조했다. 바깥쪽 선은 끝점 보정 때문에 약1.7px 기울어져 기존1px 축 조건에서 제외됐다. 인접 숫자의 축 판정에 최대2px 및 길이의0.5% 이내 기울기만 허용하고, 숫자와 대상 벽 사이에 다른 가까운 평행 벽선이 있으면 그 너머로 연결하지 않는다. 개구부의 가상 선은 물리적인 가림 선으로 사용하지 않는다. 치수 계산도 같은 작은 기울기 허용 범위를 사용한다.

289개 테스트와 빌드 통과. 실제 조건155/190에서 1750mm는 auto-wall-11의 y1104.37→1502.22, 1800mm는 auto-wall-8의 y1060.64→1502.22에 분리 연결됐다. 해당 두 값을 같은 벽에 붙여 생기던 직접 모순은 해소됐지만 다른 묶음 사이 순서 모순은 남는다. 조건125에서는1750 연결을 확보하지 못했다. 원본과 벽면 대응을 확인한 회귀 결과이며 독립적인 전체 형상 정답 검증으로 취급하지 않는다.

결과: `docs/validation/2026-09-11-wall-face-retest.json`. 실제 전체 자동 생성은0/3 유지다. 웹 배포 없이 커밋·푸시하며 전체 목표는 미완료다.


## 손상된 PDF 문자 자동 OCR 전환과 이종 도면 검증

PDF 문자 항목이 존재해도 제어문자·대체문자·사설 영역 문자가 3개 이상이면서 비공백 문자의 1% 이상이면 해당 문자층을 재사용하지 않고 로컬 OCR로 전환한다. 정상 다국어와 소수 특수기호는 유지한다. 사용자 확인 절차나 외부 API는 추가하지 않았다. 이 검사는 명백한 인코딩 손상 감지이며 모든 잘못된 문자 매핑을 판별하지는 못한다.

공식 Coningsby 자료의 7페이지를 추가했다. 렌더링 확인 결과 평면도가 아니라 두 층의 사시도이므로 기존 평면도 3페이지 성공률과 분리한다. 문자196개 중 손상52개를 검출해 OCR로 전환했고 숫자 후보는2개뿐이었다. 사시도의 임의 각도 숫자·곡면·층별 분리는 현재 지원이 부족하며 전체 공간은 생성하지 않았다. 기존 실제 평면도 전체 생성0/3, 추가 사시도0/1이며 Espacio 선택 숫자6/7 회귀 결과는 유지됐다. 페이지7은 검증 매니페스트로 선택했으므로 자동 페이지 선택 성공으로 취급하지 않는다.

자료 출처와 SHA-256, 선택 페이지를 `docs/validation/plan-corpus.json`에 고정했다. 결과는 `docs/validation/2026-09-11-pdf-text-quality-retest.json`. 다음 구조 구현은 평면도/사시도·복수 층 구분과 벽 경계 복원에 집중한다. 웹 배포는 보류한다.


## 짧은 꺾임 벽 연결선 보존

Espacio 2페이지 상단256/40/480 표기 사이 꺾임의 검출선을 원본과 비교했다. 조건155에서 모서리 보정 후 세로 연결선 길이가9.43px로 줄어 기존10px 필터에서 제거되고 연결된 두 선도 함께 탈락했다. 10px 미만 선은 서로 다른 두 굵은 벽이 양 끝을1px 이내에서 지지할 때만 후보로 남긴다. 기존의 열린 가지 제거·긴 구조와의 연결 조건은 유지한다. 떨어진 짧은 선이나 같은 벽에 붙은 작은 돌기는 보존하지 않는다.

조건155에서 x1068.86/y517.03→526.46의 짧은 연결선과 수평·수직 이어진 벽이 복원됐다. 조건125/190에서는 복원되지 않아 다중 조건 일치 기준은 아직 미달이다. 실제 평면도 전체 생성0/3 및 사시도0/1은 유지된다. 이 구간의 실제 검출 좌표를 전체 구조 처리 회귀 테스트로 고정했다. 검증 결과는 `docs/validation/2026-09-11-short-connectors-retest.json`. 웹 배포 없이 커밋·푸시한다. 전체 목표는 미완료다.


## 실제 교차점의 작은 선 끝 돌출 보정

Espacio 2페이지 조건190에서는 짧은 세로선의 시작이 수평 벽보다9.43px 돌출되어 기존 모서리 허용 범위를 벗어났다. 실제 검출선 내부의 교차점에 한해 가까운 끝에서 최대12px를 줄일 수 있도록 했다. 선 밖으로 연장하는 허용 범위는 기존 획 두께 기준을 유지한다. 경쟁 교차점은 보류하고 선 전체를 없애는 보정도 적용하지 않는다.

실도면 재검증에서 상단 꺾임의 연결선3개가 조건155와190에서 남았다(직전155만). 조건125에서는 최초 작은 세로선이 검출되지 않아 여전히 복원되지 않는다. 이 국소 구간의 두 조건 복원은 전체 도면 일치/완성을 뜻하지 않는다. 기존 Espacio 1페이지의 닫힌 경계 조건125/155는 유지됐으며 실제 평면도 전체 생성0/3, 추가 사시도0/1이다.

296개 테스트 및 빌드 통과. 실제 검출 좌표를 사용한 전체 구조 처리 테스트와 작은 돌출/실제 틈 구분 테스트를 추가했다. 결과: `docs/validation/2026-09-11-corner-cap-retest.json`. 웹 배포 없이 커밋·푸시하며 목표는 계속 진행 중이다.


## 사선 경계의 출입구 후보와 접근 벽 보존

기존 모서리 출입구는 수평·수직 틈과 직각 연결만 허용했다. 사선 틈에서 한쪽 벽 방향이 틈 방향과5도 이내이고 두 벽 모두 틈 바깥으로45도 이내를 향하면 표기와 실제 끝점으로 후보를 구성한다. OCR 신뢰도90 이상, 표기가 틈 사이에 위치, 유일한 후보, 기존 틈 차단선/경쟁 표기 배제 조건을 유지한다. 다른 선의 끝이나 내부에 이미 연결된 끝점은 이 사선 규칙에서 제외한다. 출입구를 지지하는 짧은 접근 벽 보존에도 같은 규칙을 사용한다.

실제 Espacio 2페이지 Back Door가 조건125/155에서 검출됐다. 아래 끝(66,826.56)과 위 끝(약97.2,731.89)으로 두 결과의 끝점 차이는0.03px 미만이다. 조건190은 미검출이며, 물리적인 문 폭과 전체 구조 완성을 확인한 것은 아니다. Espacio 1페이지의 기존 문/창문/계단 접근과 조건125/155 닫힌 경계는 유지됐다. 실제 평면도 전체 생성0/3 및 사시도0/1은 그대로다.

297개 테스트와 빌드, 실제 PDF 재검증 통과. 결과는 `docs/validation/2026-09-11-oblique-door-retest.json`. 웹 배포 없이 커밋·푸시하며 목표는 계속 진행 중이다.


## 겹치는 수치 교차점으로 인한 전체 분할 중단 수정

실제 Espacio 2페이지의 구조는 세 조건 모두 `splitWallJunctions`에서 중단됐다. 같은 직선의 중복 벽이 원인이 아니라, 여러 교차 계산의 미세한 차이가 반올림 후 같은 좌표를 만들어 길이0인 조각을 생성한 것이 원인이었다. 기존1e-6 좌표 정밀도를 유지하면서 동일 좌표의 연속 교차점을 합친다. 구분 가능한 가까운 교차점은 유지하고, 입력 벽 자체가 정밀도 아래로 붕괴하면 여전히 보류한다. 원본 벽이나 실제 틈을 연장하지 않는다.

수정 후 Espacio 2페이지 조건125/155/190의 분할이 각각64/71/72개 조각으로 통과했다. 조건125/155는 이후 외곽 추출에서, 조건190은 최종 경계 판정에서 여전히 보류된다. Espacio 1페이지 조건125/155의 닫힌 경계는 유지됐다. 실제 평면도 전체 생성0/3, 사시도0/1이다. 단계별 분할 성공·순환 구조 수·외곽 선 수를 벤치마크에 추가했다. 비어 있는 입력의 분할 성공은 구조 검출 성공이 아니다.

299개 테스트와 빌드 및 실제 PDF 재검증 통과. 교차 선 순서·방향 반전, 정밀도 아래 붕괴, 가까우나 구분 가능한 교차점 테스트를 포함한다. 결과: `docs/validation/2026-09-11-coincident-junctions-retest.json`. 웹 배포 없이 커밋·푸시하며 전체 목표는 진행 중이다.


## 가구 표기와 관측 사각 외곽의 벽 후보 분리

가구 표기 종류를 추가했다. Bar/Counter/Desk/Table/Chair/Cabinet 및 대응 한국어의 독립 표기만 분류하며 숫자 단위의 bar는 가구로 처리하지 않는다. 저장 검증과 표기 UI에 연결하고 회전·확대 영역 OCR에서도 가구 표기를 보존한다. 높은 신뢰도의 표기, 중심에 가까운 위치, 관측된 네 변의 유일한 사각 외곽이 있을 때 외곽 안에 완전히 포함된 변만 벽 후보에서 제외한다. 더 길게 이어지는 공유 벽은 남긴다. 교차 구조선·복수 외곽·불완전 외곽은 보류한다. 범위 추정은 문자 상자가 아닌 선 외곽을 사용한다. 가구 높이나 실제 설치 제외 영역은 아직 생성하지 않는다.

302개 테스트와 빌드 통과. 공유 벽을 가진 합성 카운터의 세 변이 제거되고 방의 네 벽은 유지되는 전체 구조 경로를 검증했다. 실제 PDF의 Bar는 전체/회전/확대 OCR 모두에서 가구 표기로 확보하지 못했다. 따라서 실제 가구 분리가 작동했다고 주장하지 않으며 평면도 전체 생성0/3, 사시도0/1은 유지된다. 도형 안의 작은 회전 가구 문자를 국소 인식하는 개선이 남는다. 결과: `docs/validation/2026-09-11-furniture-outline-retest.json`. 웹 배포 없이 커밋·푸시하고 목표는 계속 진행한다.


## 도형 내부 국소 회전 OCR로 실제 Bar 인식

실도면의 길쭉한 사각형 안쪽을 잘라90도 회전하면 Bar가 로컬 OCR 점수96으로 읽히는 것을 확인했다. 입력 검출선에서 네 변이 확인되는 길쭉한 사각형을 찾고 획 테두리를 제외한 영역을 자동 계산한다. 문자를 찾기 위한 후보이며 도형 자체를 가구로 확정하지 않는다. 최대6개 영역에서 긴 축에 맞춘 두 방향의 OCR을 수행하고 높은 신뢰도의 가구 표기만 기존 근거에 병합한다. 기존 취소·실패 처리와 원본 좌표 복원 경로를 사용한다. API나 사용자 영역 지정을 추가하지 않았다.

자동 선택된 Espacio 2페이지 영역(x630.86,y1177.25,w60.86,h321.83)에서 Bar를 점수96으로 인식했다. 조건125/155/190 모두 가구 외곽의 세 변이 벽 후보에서 제외되어 구조 선 수가42→39,47→44,48→45로 줄었다. 공유하는 긴 아래 경계는 남는다. 계단 선 사이도 문자 탐색 후보가 되지만 가구 표기가 없으므로 가구로 분류하지 않는다. 가구의 실제 높이·설치 제외 영역은 아직 생성하지 않는다.

304개 테스트, 빌드, 실제 PDF 재검증 통과. 실제 평면도 전체 생성0/3, 사시도0/1은 유지된다. 브라우저 UI에서 이번 추가 OCR 흐름을 직접 실행하는 검증은 아직 하지 않았다. 결과는 `docs/validation/2026-09-11-shape-label-ocr-retest.json`. 웹 배포 없이 커밋·푸시하며 전체 목표는 계속 진행한다.


## 국소 OCR 브라우저 검증과 보류 안내 수정

로컬 http://127.0.0.1:5180/ 에서 실제 Espacio 2페이지 렌더 PNG(2400×1696)를 도면 불러오기로 입력했다. 자동 분석 후 숫자 표기30개, 구조 선 후보500개가 표시됐고 공간 자동 적용은 보류됐다. 참고 도면 배치 후 표기 목록에서33번째 가구 Bar와 OCR 점수96/100을 확인했다. 콘솔 오류·경고는 없었다. 이는 이미지 입력의 브라우저 검증이며 PDF 다중 페이지 선택·전체 자동 공간 생성 성공을 뜻하지 않는다.

보류된 도면 저장 후 ‘두 점 축척 보정 후 벽을 맞춰 주세요’라고 요구하던 알림을 ‘도면과 인식 결과를 저장했습니다. 공간 자동 생성은 아직 보류 상태입니다.’로 바꿨다. 가져오기 대화상자도 직접 보정 유도 대신 인식 결과 저장을 안내한다. 자동 생성 성공으로 표현하거나 백그라운드 재처리를 약속하지 않는다. 수정 후 동일 브라우저에서 다시 가져오기·저장해 새 안내를 확인했다.

두 적용 모두 실행 취소로 이전 테스트 프로젝트(벽5개·작품1개·기존 계단 표기1개)를 복원했다. 변경 후 빌드 통과. 문구 변경이므로 추가 단위 테스트는 만들지 않았으며 직전304개 테스트를 이번에 재실행했다고 주장하지 않는다. 웹 배포 없이 커밋·푸시한다. 전체 평면도 자동 생성0/3 상태와 목표는 유지된다.


## 평행 벽을 기울이던 끝점 병합 수정

얇은 안뜰 경계의 검출과 보존을 확인한 뒤, Espacio 2페이지 영상 설치 공간의 두 평행 선 중 한쪽 끝만 가까운 선으로 합쳐지는 현상을 찾았다. 마지막4px 끝점 병합이 원래 수직인 x1110 선의 끝을 x1111.71로 이동시켜 얇은 삼각형을 만들었다. 수평·수직 검출선은 동일 축 방향(0.01px 이내)을 유지하는 끝점에만 병합하도록 수정했다. 실제 교차점 보정은 앞선 처리에 그대로 맡기고 가까운 두 벽을 임의로 하나로 합치지 않는다.

실제 조건155에서 두 선이 각각 x1110과x1111.71로 수직을 유지하며 외곽 추출27개 선이 반환됐다(직전 외곽 추출 보류). 최종 공간 판정은 여전히 실패한다. Espacio 1페이지의 조건125/155 닫힌 경계는 유지됐다. 실제 평면도 전체 생성0/3, 사시도0/1이다. 외곽 추출 반환 자체를 원본 경계 정답으로 취급하지 않는다.

305개 테스트와 빌드 및 실도면 재검증 통과. 한쪽 끝만 가까운 부분 평행벽의 방향 보존 테스트를 추가했다. 결과는 `docs/validation/2026-09-11-axis-preserving-snap-retest.json`. 웹 배포 없이 커밋·푸시하며 목표는 진행 중이다.


## 굵은 부분과 얇은 연장의 중복 검출 분리

Espacio 2페이지 상단 경계에서 긴 얇은 선(y522.18)과 굵은 부분 선(y521.32)이 겹쳐 외곽 바깥에 중복 가지가 남았다. 축 방향이 같고 중심 차이1px 이내, 겹침30px 이상, 얇은 연장10px 이상이며 대응이 유일할 때 겹치는 얇은 구간을 제외한다. 굵은 부분의 길이와 검출 두께는 유지하고 남은 얇은 연장은 그대로 둔다. 가까운 평행벽 전체를 합치거나 긴 얇은 구간 전체를 두껍게 하지 않는다.

분리 후 짧아진 굵은 구간이 모서리 잡선 때문에 탈락하는 문제도 확인했다. 짧은 굵은 선은 긴 굵은 구조선과 실제로 교차할 때만 작은 돌출을 줄인다. 최대12px 및 상대 획 두께/2+3px 이내, 실제 선 범위 안의 유일 교차점만 사용하며 틈을 연장하지 않는다. 짧은 잡선은 이 보정의 지지선으로 쓰지 않는다.

조건155에서 상단 경계가 x147.97→265.71과265.71→437.14, 동일y522.18로 연결됐다. 기존 Back Door 조건125/155와 Espacio 1페이지의 닫힌 경계 조건125/155는 유지됐다. 실제 평면도 전체 생성0/3, 사시도0/1이며 다른 외곽 돌출·구조 의미·치수 미결정 문제는 남는다.

309개 테스트와 빌드 및 실제 PDF 재검증 통과. 결과는 `docs/validation/2026-09-11-coincident-strokes-retest.json`. 웹 배포 없이 커밋·푸시하며 전체 목표는 계속 진행한다.


## 얇은 선끼리 실제 교차하는 모서리의 돌출 정리

얇은 획이 포함된 모서리는 두 검출선 내부의 실제 교차점으로만, 양쪽 가까운 끝에서 각각4px 이내일 때 줄일 수 있게 했다. 선을 연장해 틈을 채우지 않는다. 굵은 선끼리 이미 제안된 모서리 끝점에는 얇은 보조선의 제안이 개입하지 못하게 해 기존 사선 공간 복원 회귀를 방지했다. 유일한 교차점과 선 붕괴 방지 조건은 유지한다.

실제 Espacio 2페이지 조건155의 아래 가로선 시작이x65.14→66으로 정리됐고, 오른쪽 세로선 끝은y1503.59→1502.22로 바닥선에 맞았다. 아래 가로선 오른쪽 끝x2123.14는 교차점x2119.71보다 약3.43px 돌출된 상태로 남는다. 최종 공간 판정은 여전히 실패한다. Espacio 1페이지 조건125/155 닫힌 경계와 기존 사선 합성 공간 테스트는 유지됐다. 실제 평면도 전체 생성0/3, 사시도0/1이다.

310개 테스트·빌드·실제 PDF 재검증 통과. 얇은 실제 교차점 보정, 작은 실제 틈의 연장 금지, 큰 돌출 보류 테스트를 추가했다. 결과: `docs/validation/2026-09-12-thin-corner-retest.json`. 웹 배포 없이 커밋·푸시하며 목표는 계속 진행 중이다.


## 관측 끝점의 후속 보정과 호환되는 중복 조각 정규화

Espacio 2페이지 하단 가로선은 확대 배율에서 굵은 선 기준에 들어가 기존 긴 선 보정 제외 조건에 걸렸다. 긴 선 또는 약한 지지선은 다른 검출선의 실제 끝점에서만4px 이하 돌출을 줄일 수 있도록 했다. 내부 보조선 교차는 근거로 삼지 않는다. 첫 보정에서 만들어진 끝점을 한 번 더 사용할 수 있게 총2회로 제한한 보정을 적용한다. 선 밖 연장은 하지 않는다.

조건190에서 보정 후 같은 직선의 부분 중복이 드러나는 회귀가 발생해 교차 분할을 보완했다. 높이·두께·역할·색·가시성·잠금 상태가 같은 정확한 공선 겹침은 모든 겹침 끝점에서 분할하고 같은 조각을 제거한다. 속성이 다르면 보류하며 평행하지만 떨어진 벽은 합치지 않는다. 자동 구조 원본의 치수 표기·선 근거는 삭제하지 않는다.

Espacio 2페이지 조건155에서 오른쪽 아래 가로선 끝이x2123.14→2119.71로 맞춰졌고 처음으로 `topologyResolved=true`를 얻었다. 조건125/190은 실패한다. 이 결과는 닫힌 그래프와 내부 분기 판정 통과이며 실제 안뜰/계단/시설의 의미나 모든 벽의 정답 일치를 검증한 것은 아니다. 치수 좌표가 결정되지 않아 `constrainedVenueReady=false`, 전체 평면도 자동 생성0/3, 사시도0/1이다. 기존 Espacio 1페이지 조건125/155 닫힌 경계는 유지됐다.

312개 테스트, 빌드, 실제 PDF 재검증 통과. 실제 끝점 지지·틈 연장 금지·제한된 후속 보정·부분 및 역방향 중복·충돌 속성 보류를 검증했다. 결과는 `docs/validation/2026-09-12-supported-endpoint-retest.json`. 웹 배포 없이 커밋·푸시하고 목표는 계속 진행한다.


## 끊긴 분기 주변의 부분 치수 오연결 방지

Espacio 2페이지의 285 cm 표기가 영상 설치 공간의 일부가 아니라 전체 하단 벽에 연결되는 오류를 확인했다. 다른 벽의 끝이 8px 이내에서 떨어진 경우 가능한 부분 구간을 모호성 판정에만 사용한다. 실제 접합이 없으므로 그 구간을 측정값으로 확정하거나 벽을 연장하지 않는다. 거의 평행한 이중선의 작은 래스터 기울기는 분기 근거에서 제외한다.

실제 PDF 재검증에서 조건155의 전체 하단 벽 2850mm 오연결이 제거됐다. x축은 순서 충돌 없이 미결정 상태이며, z축에는 동일 구간의 1800/2250mm 충돌이 남는다. Espacio 1페이지 조건125/155 및 2페이지 조건155의 닫힌 경계는 유지됐다. 실제 평면도 전체 자동 생성은 여전히0/3이며 사시도 스트레스 사례는0/1이다.

313개 테스트와 빌드 및 실제 PDF 벤치마크 통과. 기존 실제 T접합의 부분 치수와 평행한 양쪽 벽면 치수 테스트도 유지됐다. 결과: `docs/validation/2026-09-12-partial-dimension-retest.json`. 다음 작업은 벽면별 치수 범위와 미결정 좌표 연결 근거 보완이다. 웹 배포 없이 커밋·푸시한다.


## 원본 픽셀 근거로 작은 접합 복원

선 검출 단계에서 짧아진 축 방향 선 끝을 수직 지지선 중심까지 복원한다. 최대6px 이내, 길이30px 이상 지지선의 실제 범위 내부, 유일한 목표 좌표이며 원본 이진 영상의 끝점부터 목표까지 모든 중심 픽셀이 어두울 때만 허용한다. 1픽셀 빈틈도 연결하지 않으며 사선·먼 거리·지지선 바깥은 제외한다. 실제 출입구 간격은 유지한다.

Espacio 2페이지 조건155의 작은 가로선 시작이 검출 영상 좌표x258→255,y819.5로 복원됐다. 영상 설치 공간 오른쪽 벽의 미결정 접합과 180/225 치수 충돌은 해결되지 않았다. 원본에서도 두 표기가 함께 존재함을 확인했으며 한쪽을 임의로 선택하지 않았다. Espacio 1페이지 조건125/155와 2페이지 조건155의 닫힌 경계는 유지, 전체 평면도 자동 생성0/3과 사시도0/1이다.

316개 테스트·빌드·실제 PDF 재검증 통과. 결과: `docs/validation/2026-09-12-ink-junction-retest.json`. 웹 배포 없이 커밋·푸시한다. 전체 공간 복원과 치수 적용 범위 판정은 계속 미완료다.


## 글자 없는 계단 후보와 설치 제외 연결

반복되는 단 선5개 이상, 양쪽 실제 지지선, 일정 범위의 간격과 선 굵기를 이용해 글자 없는 계단 후보를 검출한다. 내부 격자와 명시적인 가구 표기는 제외한다. 문자 근거가 없으므로 `evidence: shape`로 보존하고 화면에는 ‘계단 추정’으로 표시한다. 독립된 선 검출 조건2개 이상에서 위치·범위가 일치해야 분석 결과에 채택하며, 다른 조건의 모순된 위치는 보류한다. 후보로 출입구를 만들어내지 않는다.

채택한 후보는 평면도 표시와 보정된 좌표의 설치 제외/작품 겹침 검사에 연결했다. 저장·다시 읽기와 비균일 좌표 변환 시 단 선 및 지지선 참조를 보존하고 누락된 참조는 거부한다. 공간 복원에서도 채택된 범위를 사용하도록 순서를 정리했다.

실제 Espacio 2페이지의 무표기 계단이 조건125/155/190 모두 검출됐다. 조건155 검출 범위는x476.57,y660.22,폭138,높이222.07px이다. 수동으로 위치를 기록한 전체 가시 계단 범위의 약77%만 덮으므로 전체 계단 외곽·높이 인식 완료로 보지 않는다. ANCA와 Coningsby에서는 새 후보가 없었으며 Espacio 1페이지의 기존 문자 기반 계단과 닫힌 경계 판정은 유지됐다. 실제 평면도 전체 자동 생성0/3, 사시도0/1은 그대로다.

321개 테스트·빌드·실제 PDF 재검증 통과. 로컬 브라우저에서 실제 PNG 업로드 후 자동 검출1개와 ‘계단 추정 · 설치 제외(검출 범위)’ 표시를 확인했다. 콘솔 경고/오류 없음, 검증 후 되돌리기로 원래 프로젝트를 복원했다. 실제 파일은 축척 미보정 상태여서 브라우저에서 물리적 겹침 정확도를 검증한 것은 아니며 좌표 변환·겹침 연결은 테스트로 검증했다. 결과: `docs/validation/2026-09-12-unlabelled-stairs-retest.json`. 웹 배포 없이 커밋·푸시한다.


## PDF 전체 미리보기 기반 분석 순서

PDF를 최대900px 미리보기로 모든 페이지 순차 검사한 뒤 구조·치수 근거가 많은 페이지부터 정밀 OCR/공간 검증을 수행한다. 미리보기 이미지를 누적 보관하지 않으며 한 페이지 실패가 뒤의 페이지를 막지 않는다. 잘못된 순위나 미리보기 실패가 있어도 정밀 분석 대상으로 남긴다. 확정된 공간을 찾을 때만 조기 종료하며, 없으면 모든 페이지를 분석해 미확정 후보를 반환한다. 따라서 공간이 전부 미확정인 PDF의 전체 OCR 시간을 줄였다는 주장은 하지 않는다.

실제 Coningsby 정보집의 사진 표지 창틀·난간이 벽처럼 검출돼200점으로 우선되는 문제를 확인했다. 밝은 배경 비율, 중간 명도 및 색상 비율로 사진/짙은 배경의 선 점수 가중치를 줄였다. 짙거나 색이 있는 실제 도면도 탈락시키지 않고 뒤에서 분석한다. 평면도/사진/사시도를 확정 분류하는 기능은 아니다.

실제 원본3개, 전체28페이지 미리보기 검증을 추가했다. Coningsby25페이지 미리보기는 로컬 Node 렌더러에서 약4초, 사시도7페이지는3번째 후보(0점 동률)가 됐다. ANCA1페이지, Espacio2페이지도 검사했고 Espacio는2→1순이다. 브라우저 실제 PDF 업로드에서도 자동 선택 상태로2페이지 모두 정밀 분석해2페이지 후보를 표시했으며 숫자30개·계단 추정1개, 공간 생성은 보류됐다. 콘솔 경고/오류는 없었고 가져오기를 취소해 기존 프로젝트를 유지했다.

326개 테스트·빌드·실제 전체 페이지 미리보기 벤치마크 통과. 뒤쪽 검증 가능 페이지 우선 처리, 잘못된 순위/실패 후 계속 분석, 사진 후보도 최종 검증 허용, 취소 및900px 렌더·해제 동작을 확인했다. 결과: `docs/validation/2026-09-12-page-preview-ranking.json`. 여러 층 동시 구성과 방향·영역 자동 판정, 전체 실도면 복원은 미완료이며 웹 배포 없이 커밋·푸시한다.


## 정상 PDF 문자층과 이미지 설비 표기의 혼합 인식

Paragon Hall 공식 다운로드의 Fire Exit & Fire Hose 도면을 검증 자료로 추가했다. 출처와 SHA256은 corpus manifest에 고정했으며 원본 PDF는 임시 폴더에만 보관한다. 이 자료는 큰 전시장·설비 표지·많은 식별번호·색상 선을 포함한 소방 오버레이이며 치수가 완비된 복원 정답은 아니다.

문자층이 정상이라는 이유로 OCR을 생략하면서 이미지로 삽입된 비상구·소방 표지를 놓치는 문제를 확인했다. 정상 PDF의 기존 숫자·단위는 보존하고 전체 및4개 겹치는 구역의 로컬 OCR로 설비 표기만 추가한다. 구역 OCR은 최대2배/3600px, eng+kor를 사용하며90점 이상만 채택한다. 보완 실패 시 원래 문자 근거를 보존하고 취소는 즉시 중단한다. 겹치는 표기는 중복 제거하며 새 ID의 충돌을 피한다. 명시적인 FIRE HOSE REEL 표기도 소화전 계열로 분류하지만 그림 자체를 분류한 것은 아니다.

별도 Node 실제 PDF 검증에서 큰 초록색 비상구 이미지 표지7개의 영역 내 인식은0→5개였다. 기존 문자층의 작은 Fire Exit3개는 별도 표기이므로 이 수치를 실제 출입구 수로 합산하지 않는다. 로컬 브라우저는 같은 PDF에서 기존3개+이미지4개(숫자2개 포함 총9표기)를 보였으며 Node와 차이가 있다. 소방호스 그림 표지 인식은0개, 에어컨 사례 검증은 아직 없고 설비의 실제 점유 범위도 미해결이다.

330개 테스트·빌드·확장된 실제 PDF 벤치마크 통과. 브라우저 콘솔 경고/오류는 없었고 되돌리기로 원래 프로젝트를 복원했다. 기존 Espacio 닫힌 경계 판정은 유지됐다. 새 소방 오버레이를 포함한 평면 도면 전체 자동 생성은0/4, 사시도는0/1이며 선택한 이미지 표지 정답만으로 전체 설비 정확도를 주장하지 않는다. 결과: `docs/validation/2026-09-12-hybrid-facilities-retest.json`, 정답 범위: `docs/validation/paragon-selected-facilities.json`. 웹 배포 없이 커밋·푸시한다.


## PDF 원본 표지 이미지 해상도 복원

페이지 렌더링에서 작아지는 비상구 표지를 PDF 내장 이미지에서 원본 해상도로 추출하고 회전·이동 행렬을 복원한다. 작은 컬러 이미지 중 렌더링된 페이지와 표본 색상이 일치하는 후보만 읽는다. 이 비교는 근사적인 가시성 확인이며 PDF 클리핑·선택 레이어 전체를 해석한 것은 아니다. RGB/RGBA 및 bitmap 이미지를 지원하고 동일 이미지의 OCR은 재사용한다. OCR 결과는 도면 좌표로 되돌려 기존 숫자·단위를 보존하면서 고신뢰 설비 표기만 병합한다. 추출 실패 시 기존 페이지 OCR을 유지한다.

Paragon 실제 PDF에서 후보 이미지14개를 추출했다. 별도 Node 검증의 선택한 큰 초록색 비상구 표지7개는 기존5/7에서7/7로 개선됐다. 브라우저도 이미지 표지4개에서7개로 늘었으며 기존 문자층3개·숫자2개를 포함해 총12표기였다. 문자층 표기와 이미지 표지는 같은 문을 가리킬 수 있으므로 실제 출입구 수로 합산하지 않는다. 소방호스 표지 인식은 여전히0개다. 원본을 읽어도 작은 캡션이 확정되지 않았으며 언어를 영어만으로 바꾼 진단도 개선하지 못했다.

변환 행렬·반복 이미지 OCR 재사용·좌표 복원 테스트와 실제 PDF 벤치마크, 브라우저 배치/되돌리기를 검증했다. 브라우저 경고/오류는 없었고 원래 프로젝트를 복원했다. 기존 닫힌 경계 판정은 유지됐으며 전체 자동 생성은 평면 도면0/4, 사시도0/1로 미완료다. 결과: `docs/validation/2026-09-12-native-sign-retest.json`. 다음 우선 과제는 벽 경계와 치수 적용 범위의 충돌 해결, 전체 공간 복원 성공 사례 확보이며 표지 캡션 보완·설비 점유 범위 연결도 남아 있다. 웹 배포 없이 커밋·푸시한다.
